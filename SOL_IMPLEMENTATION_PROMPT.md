# Sol 구현 인계 프롬프트

아래 내용을 다음 Sol 세션에 전달한다.

```text
너는 이 프로젝트의 백엔드 구현 담당이다. 합의된 계획을 설명하는 데 그치지 말고 현재 브랜치의 1차 구현 단위를 실제 코드와 검증까지 완성해줘.

작업 위치: /Users/hurdoo/coding/projects/feature-be-recommendation-core

먼저 사용자 제공 AGENTS.md 지침, 저장소의 적용 가능한 AGENTS.md, DEVELOPMENT_GUIDE.md, BACKEND_MVP_PLAN.md를 읽어라. BACKEND_MEETING_DECISIONS.md와 BACKEND_RECOMMENDATION_PLAN.md는 배경으로 참고하되 충돌하는 제품 범위는 BACKEND_MVP_PLAN.md가 최신이다. 실제 코드·git 상태·기존 테스트를 확인하고 사용자 변경을 보존해라.

전체 목표는 전국 최대 10개 장소 추천 + 날씨·실내외 적합도 + 서울 일부 실시간 혼잡도 + 관련·대체 장소 추천이다. 로컬은 SQLite이며 운영 DB는 미정이다. TMAP은 나중에 연결하고 지금은 직선거리와 이동시간 null을 사용한다. 날씨를 후속으로 미루거나 추천을 3개/서울 전용으로 구현하지 마라.

이번 브랜치에서는 최종 계획 7절의 1~3 단계, 즉 수집 계약 확인·전국 적재/수집 작업/최신성 기반·날씨/실내외 기반·핵심 추천 API와 검증을 완료해라. 기존 places/users/수집 명령을 재사용하고 확장해라. 관련·대체 추천 및 행동·피드백은 후속으로 연결 가능한 구조와 남은 작업을 문서화하되 이번 핵심 기능을 밀어내지 마라.

신규 공급자는 공식 명세에서 필드·발표 주기·한도·대상 범위를 확인하고 응답을 추측하지 마라. 실제 키 값은 출력·기록하지 마라. 키가 없거나 실호출이 불가능하면 확인된 명세와 비밀 없는 fixture로 구현·테스트를 진행하고 검증하지 못한 부분만 명시해라. 대량 수집 전 예상 호출량을 계산하고 처음에는 제한된 범위로 실검증해라.

정기 수집과 보충 수집은 공통 워커/서비스를 사용하고 추천 계산기에서 공급자를 직접 호출하지 마라. SQLite 워커는 1개로 시작한다. 중복 작업 방지·실패 재시도·호출 예산·재시작 복구·2초 총 보충 대기·원래 관측/발표 시각 기준 만료 처리를 구현해라. API 인증정보·원본 비밀·실제 DB를 Git에 넣지 마라.

추천은 기본/상한 10개, 후보 부족은 정상 응답, 조건 자동 완화 금지다. 날씨·혼잡도·실내외 미확인 정보를 추측하지 말고 해당 점수를 제외해라. 필수 조건과 선호를 구분하고 미래 방문에 현재 혼잡도를 미래 상태처럼 적용하지 마라. 가중치·임계값은 설정으로 분리하고 데이터 비중을 정확도라고 부르지 마라.

단계별로 의미 있는 검증을 수행하고 기존 장소·인증 기능의 회귀를 확인해라. API 명세·개발용 시드·실행 방법·새 환경변수 이름·완료/미완료 내역을 갱신해라. 운영 DB 선택, 공개 배포, 자동 데이터 삭제, 미확정 개인정보 수집은 수행하지 마라. 푸시/PR 병합은 이번 요청에 포함하지 않는다. 향후 푸시 전에는 사용자 지침에 따라 hurdooagent 계정의 푸시 방식을 확인해야 한다.

마지막에는 구현 내용, 실행/테스트 결과, 실 API 검증 범위, 남은 계약 및 후속 작업을 간결하게 보고해줘.
```
