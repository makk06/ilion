# 서울 혼잡도 121개 영역 확대 검증 (2026-09-13)

서울 열린데이터광장의 [실시간 인구데이터 공식 목록](https://data.seoul.go.kr/dataList/OA-21778/F/1/datasetView.do)에 첨부된 `서울시 주요 121장소 목록.xlsx`(수정일 2026-04-14, SHA-256 `60aedf332efef1535623e22c14af2acd6b3ccfa35e60423fbbea8cc8188f1ff7`)에서 `AREA_CD`, `AREA_NM`, `CATEGORY`를 추출했다. 유효한 `POI` 코드는 121개이고 중복이 없다. 구성은 관광특구 7, 고궁·문화유산 5, 인구밀집지역 48, 발달상권 28, 공원 33개다. 기존 12개 코드·이름·분류는 공식 목록과 모두 일치해 카탈로그 맨 앞에 그대로 두고 나머지 109개를 추가했다.

기존 SQLite를 `/private/tmp/feature-be-recommendation-core-before-seoul-121-20260913-004053.sqlite3`에 백업했고 무결성 검사 결과는 `ok`였다. 공통 작업 큐에 `full121-20260913004102` 창으로 121개 `seoul_crowd` 작업을 등록해 한 번 실행했다. **121개 모두 성공**, 관측 121개 영역 적재, 서울 호출 예산 정기 레인 121회 기록을 확인했다. 워커 출력은 `/private/tmp/seoul-121-worker-20260913-004102.log`에 보관했다. 수집 후 SQLite 무결성 검사도 `ok`였다.

| 항목 | 결과 |
| --- | --- |
| 서울 혼잡 영역 | 12 → 121개 |
| 관광 장소와 검증된 혼잡 영역 매핑 | 13곳 유지 |
| 공급자 관측 시각 | 2026-09-13 00:10 KST, 121곳 동일 |
| 수집 시각 | 00:41:09~00:41:21 KST |
| 수집 직후 관측 나이 | 약 31.6분 |
| 현행 30분 추천 유효 영역 | 0/121 |
| `REPLACE_YN` 대체 관측 | 0/121 |

영역 수집 확대는 서울 내 관측 범위를 넓히지만, 아직 매핑되지 않은 관광 장소의 추천 결과를 바꾸지는 않는다. 공급자 지연도 해소하지 않는다. 공식 API는 [한 번에 한 장소씩 조회](https://data.seoul.go.kr/dataList/OA-21778/F/1/datasetView.do)하므로, 현재 `run_data_worker --dev`의 15분 예약을 121곳 전체에 상주시킨다면 정기 요청만 **하루 11,616회**다. 이번에는 일회성 워커만 실행했고 상주 워커·예약 작업을 시작하지 않았다. 지속 수집 주기와 관측 보관량은 지연 분포와 이용 가치를 측정한 뒤 정한다. 로컬 DB만 변경했고 푸시·배포는 하지 않았다.
