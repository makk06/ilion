# 서울 혼잡도 45분 시험 기준과 일회성 재수집 (2026-09-13)

## 적용한 기준

서울 혼잡 관측은 15분 이내를 신선, 15~30분을 일반 사용, 30~45분을 지연 관측으로 분류한다. 지연 관측은 원래 혼잡 가중치 0.20의 절반인 0.10으로 추천 점수의 분자와 분모에 반영한다. `data_coverage`에도 낮아진 가중치를 쓴다. 관측 시각과 `is_delayed=true`, `score_weight_factor=0.5`를 응답에 표시한다. `quiet_required=true`에서는 지연 관측으로 한산함을 확정하지 않는다. 45분을 초과하면 추천 점수와 장소 목록의 혼잡도 필터에서 제외한다. 이 기준은 공급자 지연에 대응하는 **시험 정책**이며 혼잡 정보의 실제 정확도를 보증하지 않는다.

## 일회성 수집 결과

기존 SQLite를 `/private/tmp/feature-be-recommendation-core-before-crowd45-20260913-010250.sqlite3`에 백업했고 백업본 무결성은 `ok`였다. `crowd45-20260913010256` 창으로 서울 공식 121개 영역의 작업 ID 587~707을 등록해 공통 워커를 한 번 실행했다. 워커 로그는 `/private/tmp/seoul-crowd45-worker-20260913-010256.log`에 남겼다. 01:03:02~01:03:14 KST에 **121/121건 성공**, 처리 121건, 재시도·실패 0건이었다. 수집 후 SQLite `PRAGMA quick_check`도 `ok`였다.

| 항목 | 측정값 |
| --- | --- |
| 공급자 관측 시각 | 00:30 KST 39곳, 00:35 KST 82곳 |
| 01:03:21 KST 관측 나이 | 28.7~33.7분 |
| 같은 시각 0~30분 일반 사용 | 82곳 |
| 같은 시각 30~45분 지연 사용 | 39곳 |
| 같은 시각 만료 | 0곳 |
| 대체 관측(`REPLACE_YN`) | 0곳 |
| 관광 장소와 검증된 영역 매핑 | 장소 13곳, 영역 12곳 유지 |

새 코드로 개발 서버를 다시 띄운 뒤 `GET /healthz`와 `GET /test/backend/`가 200을 반환했다. 테스트 페이지는 121개 중 121개 추천 사용 가능, 39개 지연으로 표시했다. 실제 `POST /api/recommendations`에서 덕수궁은 `mvp-3`의 지연 관측으로 반환됐고, 혼잡도는 순위 기여 항목에 포함됐으며 `score_weight_factor=0.5`, `data_coverage=0.4`였다. `POST /test/backend/` 미리보기에서도 혼잡 근거 1개와 “지연 관측 · 점수 비중 절반” 표시를 확인했다. 같은 위치의 `quiet_required=true` 요청은 지연 관측을 한산함 근거로 받아들이지 않아 후보 0개를 반환했다. 전체 테스트 95개와 Django 설정·마이그레이션 검사도 통과했다.

첫 API 점검 때 기존 8000번 서버가 아직 `mvp-2` 코드를 제공해 보충 수집 작업 1건(덕수궁 영역 `POI009`, ID 708)을 등록했다. 기존 서버를 종료하고 새 코드로 다시 띄웠으며, 해당 작업도 성공 처리해 대기 작업 0건으로 정리했다. 따라서 이 검증 과정의 서울 API 호출은 정기 121회와 보충 1회, 총 122회다.

이번 수집은 한 시점의 가용성을 입증한다. 00:30 관측은 01:15 이후, 00:35 관측은 01:20 이후 만료되므로 상주 워커가 없는 현재 개발 서버에서는 테스트 페이지의 혼잡 근거가 다시 줄어든다. 공급자 지연 분포와 추천 유용성은 여러 시간대에 걸쳐 측정해야 한다. 로컬 DB와 코드만 변경했고 푸시·배포하지 않았다.
