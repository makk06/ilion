# ILION 결정 기록

기능, 데이터 구조, 외부 연동 범위를 변경할 때 이 문서에 날짜, 결정 내용,
이유, 영향 범위와 담당 영역을 기록합니다. 실제 인증키와 비밀번호는 기록하지
않습니다.

## 2026-08-17: 장소 및 혼잡도 데이터 연동 범위

- 상태: 확정
- 결정:
  - 서비스의 장소 범위는 전국으로 한다.
  - 한국관광공사 국문 관광정보 서비스(`KorService2`)를 전국 장소의 첫 번째
    외부 공급자로 사용한다.
  - 혼잡도는 우선 서울 실시간 인구데이터(`citydata_ppltn`)가 제공하는 서울
    주요 장소 일부에만 제공한다.
  - 서울 외 지역과 아직 매핑되지 않은 장소의 혼잡도는 없는 값으로 처리한다.
- 이유:
  - 전국 장소 탐색을 유지하면서도 이용 가능한 실시간 혼잡도 데이터부터
    단계적으로 연결하기 위함이다.
- 영향 범위: 장소 수집, 장소 조회, 추천 입력 데이터, 배치 작업
- 담당 영역: `PlaceSource`, 공공데이터 연결 및 데이터 구조화

## 2026-08-17: 외부 데이터 공급자 확장 구조

- 상태: 확정
- 결정:
  - 외부 API 호출, 외부 응답 정규화, DB 저장을 분리한다.
  - `PlaceSource`는 `(source, external_id)`로 외부 장소 식별자를 유일하게
    관리하고 내부 `Place.id`와 연결한다.
  - 원본 응답, 마지막 동기화 시각과 매칭 상태를 보존한다.
  - 수집 명령은 재실행해도 중복을 만들지 않는 upsert 방식으로 구현한다.
  - 추가 공공데이터 API는 같은 인터페이스의 별도 공급자 어댑터로 추가한다.
  - TourAPI가 실내/실외를 제공하지 않으므로 해당 값을 추론하지 않고
    `Place.indoor_outdoor=unknown`으로 저장한다.
  - 내부 `Place` 필수 값이 부족한 원본은 버리지 않고 `manual_review` 상태의
    `PlaceSource`로 보존한다.
- 이유:
  - 사용할 외부 API 목록이 아직 확정되지 않았으며, 공급자별 응답 차이가
    내부 장소 모델과 수집 흐름에 퍼지는 것을 막기 위함이다.
- 영향 범위: `places` 앱의 모델, 외부 API 클라이언트, 정규화 서비스, 테스트
- 담당 영역: 공공데이터 연동

## 2026-08-17: 혼잡 지역과 개별 장소의 분리

- 상태: 확정
- 결정:
  - 서울의 혼잡도는 개별 관광지가 아닌 `AREA_CD` 기반 영역 데이터로 본다.
  - `CrowdArea`가 외부 혼잡 영역을 나타내고 `CrowdData`는 `CrowdArea`를
    참조한다.
  - 개별 `Place`와 혼잡 영역은 `PlaceCrowdArea` 매핑으로 연결한다.
  - 최초 구현에서는 검증된 장소만 관리 명령으로 명시적으로 매핑하며,
    좌표·영역 기반 자동 매핑은 별도 검증 후 추가한다.
- 이유:
  - `광화문·덕수궁`처럼 하나의 혼잡 영역에 여러 개별 장소가 포함될 수 있어
    `CrowdData`를 하나의 `Place`에 직접 연결하면 데이터 의미가 왜곡된다.
  - 이후 다른 지자체의 영역 기반 혼잡도 공급자를 추가할 수 있어야 한다.
- 영향 범위: 혼잡도 모델, 장소-혼잡도 조회, 추천 알고리즘 입력
- 담당 영역: 공공데이터 구조화

## 2026-08-17: 첫 번째 구현 완료 범위

- 상태: 확정
- 포함:
  - `PlaceSource`, `CrowdArea`, `PlaceCrowdArea`, `CrowdData`
  - TourAPI 전국 장소 수집·정규화·저장
  - 선택한 서울 지역의 실시간 인구 및 혼잡도 수집·저장
  - Django 관리 명령, dry-run/범위 제한 옵션, fixture 기반 테스트
- 제외:
  - 사용자·인증 기능
  - `ForecastCrowd`, `WeatherSnapshot`, 자체 혼잡도 예측
  - 추천 점수 및 사용자 대상 추천 API
  - Redis, Celery와 운영 스케줄러
- 이유:
  - 외부 데이터가 실제 DB까지 저장되는 첫 번째 수직 흐름을 완성하면서,
    아직 선택되지 않은 API에 구조가 종속되는 것을 피하기 위함이다.
- 영향 범위: 백엔드 장소 및 공공데이터 영역
- 담당 영역: 공공데이터 연동

## 2026-08-17: 외부 API 인증정보 관리

- 상태: 확정
- 결정:
  - 로컬 개발용 키는 `tourist_congestion_backend/.env`에 저장한다.
  - 공유 가능한 변수 이름은 `.env.example`에 기록한다.
  - Django는 `python-dotenv`로 백엔드 루트의 `.env`를 읽는다.
  - 이미 설정된 운영체제/배포 환경 변수는 `.env`로 덮어쓰지 않는다.
  - 실제 키는 Git, 문서, 이슈, 채팅에 기록하지 않는다.
- 현재 필요한 변수:
  - `TOUR_API_SERVICE_KEY`: 한국관광공사 국문 관광정보 서비스_GW
  - `SEOUL_OPEN_API_KEY`: 서울 열린데이터광장 일반 인증키
- 영향 범위: Django 설정, 로컬 실행, 배포 환경, 보안
- 담당 영역: 백엔드 공통 설정과 공공데이터 연동

## 미확정 사항

- 서울에서 최초 수집할 혼잡 지역 목록과 수집 주기
- 서울 실시간 인구데이터 외에 추가할 공공데이터 API 목록
- 날씨와 예측 데이터의 공급자 및 저장 주기
- 운영 배치 실행 방식과 API별 호출 한도 정책

## 2026-08-17: 적재 데이터의 환경별 공유 방식

- 상태: 확정
- 결정:
  - 로컬 `db.sqlite3`와 전체 공공데이터 덤프는 Git으로 공유하지 않는다.
  - 로컬 개발자는 API 키 없이 실행 가능한 소량의 버전 관리된 개발 데이터로
    동일한 상태를 구성한다.
  - 전국 장소와 실시간 혼잡도의 전체 적재는 스테이징 또는 운영의 공용 DB에서
    한 번만 수행한다.
  - 프런트엔드와 일반 팀원은 공용 백엔드 API를 사용하며 직접 외부 API를
    호출하거나 전체 데이터를 적재하지 않는다.
  - 개발 데이터는 실제 운영 데이터와 식별 가능해야 하며 반복 적재와 정리가
    가능해야 한다.
- 이유:
  - 팀원별 중복 API 호출, 호출 한도 소모, 데이터 시점 차이와 SQLite 파일 충돌을
    방지하기 위함이다.
- 영향 범위: 로컬 개발 환경, 스테이징 DB, API 키 관리, 온보딩
- 담당 영역: 백엔드 공공데이터 연동

## 2026-08-17: 운영 초기 적재 범위 가설

- 상태: 잠정 결정(운영 전 재검토)
- 초기 장소 범위:
  - 전국 TourAPI의 활성 장소 중 관광지(12), 문화시설(14), 행사(15),
    레포츠(28), 쇼핑(38), 음식점(39)을 우선 대상으로 한다.
  - 여행코스(25)와 숙박(32)은 추천 요구가 확정되면 포함 여부를 다시 결정한다.
  - 최초에는 목록 API의 핵심 메타데이터만 저장하고 상세 API는 실제 조회·추천
    대상부터 점진적으로 보강한다.
  - 이미지 파일은 복제하지 않고 외부 이미지 URL과 출처 정보만 저장한다.
- 초기 갱신 가설:
  - TourAPI 변경분은 하루 1회 수집하고, 전체 목록 대조는 주 1회부터 시작한다.
  - 서울 혼잡도는 추천 대상과 겹치는 10~20개 영역을 15분 주기로 시작한다.
  - 혼잡도 원본 관측치는 우선 30일 보관하며 장기 분석 필요성이 확인되면
    집계 데이터와 보존기간을 별도로 설계한다.
- 재검토 기준:
  - 실제 API 호출 한도와 실패율
  - DB 용량 및 배치 실행 시간
  - 사용자가 조회하는 지역·카테고리 분포
  - 추천 품질에 기여하는 상세·혼잡도 필드
  - 서비스 공개 2주 후 또는 위 지표가 예상 범위를 벗어날 때 범위와 주기를
    다시 결정한다.
- 변경 원칙:
  - 대상 카테고리, 서울 영역 목록, 수집 주기와 보존기간은 설정값으로 관리하고
    코드에 고정하지 않는다.
  - 이 항목의 수치는 확정 운영 정책이 아니라 첫 배포를 위한 가설이며,
    측정 결과에 따라 확대·축소할 수 있다.
- 영향 범위: 운영 DB, 배치 스케줄러, API 비용·호출량, 추천 데이터
- 담당 영역: 백엔드 공공데이터 연동 및 운영

## 2026-08-17: TourAPI 상세정보의 점진적 보강

- 상태: 잠정 결정(운영 전 재검토)
- 결정:
  - 전국 목록 동기화와 장소 상세정보 동기화를 별도 작업으로 실행한다.
  - `PlaceInfo`는 `Place`와 1:1로 연결하고 소개, 연락처, 홈페이지, 대표 이미지
    URL, 운영시간, 휴무일, 태그와 원본 응답을 보존한다.
  - 상세정보는 TourAPI `detailCommon2`와 `detailIntro2`를 사용한다.
  - 상세 동기화 명령의 기본 범위는 10개, 최대 범위는 100개로 제한한다.
  - 초기 운영에서는 신규 장소, 실제 조회·추천 대상과 인기 장소부터 상세정보를
    보강한다.
  - 이미지 파일은 서버에 복제하지 않고 외부 URL만 저장한다.
- 이유:
  - 전체 장소마다 상세 API를 즉시 호출하면 개발계정 호출 한도와 초기 적재
    시간이 크게 늘어난다.
  - 목록 탐색에 필요한 핵심 데이터와 상세 화면용 데이터를 분리하면 실제 사용
    패턴에 따라 적재 범위를 조정할 수 있다.
- 재검토 기준:
  - 상세정보가 있는 장소의 조회·추천 비율
  - 외부 이미지 URL의 만료·실패율
  - API 호출 한도와 상세 배치 실행 시간
  - 서비스 공개 후 사용자가 실제로 확인하는 상세 필드
- 영향 범위: `PlaceInfo`, TourAPI 호출량, 장소 상세 API, 프런트엔드 장소 카드
- 담당 영역: 공공데이터 연동 및 데이터 구조화

## 2026-08-17: 서울 혼잡도 초기 운영 카탈로그

- 상태: 잠정 결정(운영 전 재검토)
- 결정:
  - 서울시 공식 121개 영역 전체를 즉시 적재하지 않고 관광 서비스와 직접
    연결하기 쉬운 12개 영역부터 수집한다.
  - 선택 영역과 TourAPI 장소 매핑은 버전이 표시된 JSON 설정으로 Git에서
    관리한다.
  - 초기 카탈로그는 서울시 2026-04-14 목록을 기준으로 하며 12개 영역과
    대표 장소 매핑 13개를 포함한다.
  - 한 혼잡 영역에 여러 장소가 포함될 수 있다. 예를 들어
    `잠실롯데타워·석촌호수` 영역은 롯데월드타워와 석촌호수 장소에 함께
    연결한다.
  - 설정과 매핑 규칙만 Git으로 공유하고, 실제 시간별 혼잡 관측값은 로컬 또는
    공용 운영 DB에만 저장한다.
  - 개발 시드는 3개 혼잡 영역과 2개 장소 매핑만 포함한다. 나머지 누락 항목은
    운영 적재 전 검증 결과로 표시하며 개발 오류로 취급하지 않는다.
- 초기 선택 영역:
  - 경복궁, 광화문·덕수궁, 광장시장, 북촌한옥마을, 인사동, DDP
  - 광화문광장, 국립중앙박물관·용산가족공원, 서울숲공원, 남대문시장
  - 잠실롯데타워·석촌호수, 올림픽공원
- 이유:
  - 서울 API는 한 번에 한 장소만 호출하므로 전체 영역 수집은 호출량과 실패
    지점을 불필요하게 늘린다.
  - 버전 관리된 작은 카탈로그로 수집·매핑·검증 흐름을 먼저 운영한 뒤 실제
    조회 수요에 따라 확대할 수 있다.
- 재검토 기준:
  - 혼잡도 포함 장소의 조회·추천 비율과 미매핑 요청 비율
  - 영역별 API 실패율과 호출 한도
  - 서울시 공식 영역 목록의 변경
  - 운영 시작 후 2주 또는 팀이 새로운 우선 관광권역을 정할 때
- 영향 범위: 서울 혼잡도 배치, `CrowdArea`, `PlaceCrowdArea`, 운영 DB
- 담당 영역: 공공데이터 연동 및 데이터 구조화

## 2026-08-18: 미팅용 Django 백엔드 데모 화면

- 상태: 확정
- 결정:
  - `/demo/`에 장소·혼잡도 API의 수직 흐름을 확인하는 Django 테스트 화면을
    둔다.
  - 화면은 별도 목 데이터를 만들지 않고 `places`, `nearby`, `place detail`
    읽기 API를 호출한다.
  - 검색, 서울·혼잡도 필터, 경복궁 기준 주변 장소, 장소 상세정보를 시연한다.
  - 이 화면은 팀 미팅과 백엔드 검증용이며 운영 사용자 프런트엔드는 계속
    Flutter로 개발한다.
  - 사용자·인증·추천 기능을 Django 데모 화면에 중복 구현하지 않는다.
- 이유:
  - Flutter 연동 완료 전에도 전국 장소, 서울 일부 혼잡도, 상세정보와 거리 계산이
    실제 API와 DB를 통해 연결되는 프로젝트 가능성을 한 화면에서 보여줄 수 있다.
  - 운영 프런트엔드와 데모의 역할을 분리해 임시 화면이 제품 구조를 결정하지
    않도록 한다.
- 영향 범위: Django 데모 라우트·정적 파일, 미팅 시연 절차
- 담당 영역: 백엔드 공공데이터 연동 시연
