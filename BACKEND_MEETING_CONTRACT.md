# 백엔드 통합 및 추천 기능 계약 회의안

이 문서는 장소·혼잡도 API와 사용자 기능을 통합하고, 추천 기능을 구현하기 전에
백엔드 팀이 함께 확정해야 할 계약을 정리한다. 회의가 끝났을 때 각 항목의
`결정`과 `담당자`가 채워져 있어야 한다.

현재 프로젝트의 핵심 사용자 흐름은 다음과 같이 본다.

```text
사용자가 조건 입력
→ 현재 상황에 맞는 장소 3개 추천
→ 장소가 혼잡하면 대체 장소 3개 추천
→ 지금 갈지 나중에 갈지 판단할 혼잡도 정보 제공
→ 마음에 드는 장소 저장
```

리워드, 커뮤니티, 사진 리뷰, 동행자 매칭, 복잡한 코스 생성은 이번 통합의
필수 범위로 자동 포함하지 않는다. 리워드가 공모전 서비스 설명에 반드시
필요하다면 아래 회의 항목에서 최소 구현 범위를 별도로 확정한다.

## 회의에서 먼저 확정할 핵심 8개

시간이 부족하면 아래 항목부터 순서대로 결정한다. 세부 예시와 권장안은 뒤의
해당 절을 참고한다.

1. 장소 API와 사용자 브랜치의 병합 순서 및 통합 담당자
2. 장소·추천 API의 비회원 허용 여부와 인증 범위
3. 추천 요청에 받을 위치, 반경, 혼잡도, 카테고리 조건
4. 추천 점수 가중치, 결측값 처리, 동점 정렬 기준
5. 장소 3개 추천과 혼잡한 장소 대체 추천의 API 형식
6. 실시간·예측·정보 없음의 구분과 오래된 데이터 기준
7. 리워드를 이번 MVP에 포함할지와 최소 구현 범위
8. 백엔드 완료일, 프런트 연결 가능일, 기능별 담당자

## 1 현재 구현 상태

### 장소 및 혼잡도 브랜치

`feature/be-connect-api`에는 다음 기능이 구현되어 있다.

- TourAPI 전국 장소 목록과 상세정보 수집
- 서울 실시간 인구 API 혼잡도 수집
- 장소, 상세정보, 외부 출처, 혼잡 영역, 혼잡 관측 모델
- 장소 검색, 필터, 페이지네이션
- 좌표와 반경을 이용한 주변 장소 검색
- 장소 상세정보와 최신 혼잡도 조회
- 개발 시드와 백엔드 데모 화면
- 외부 API 재시도, 타임아웃, dry-run, 인증키 로그 노출 방지

현재 공개 API는 다음과 같다.

```http
GET /api/places
GET /api/places/nearby
GET /api/places/{place_id}
```

### 사용자 기능 브랜치

별도 브랜치에는 다음 기능이 구현되어 있다.

- 이메일 회원가입과 로그인
- Google 로그인
- JWT access token과 refresh token
- 로그아웃
- 랜덤 닉네임
- 즐겨찾기 추가, 목록, 삭제

현재 장소 API 브랜치에는 사용자 앱이 포함되어 있지 않다. 추천 API, 대체 장소
API, 시간대별 혼잡도 API도 아직 없다.

## 2 회의에서 반드시 확정할 결정

회의에서는 각 항목을 논의만 하고 끝내지 말고, `결정`, `담당자`, `완료 조건`을
기록한다.

### 2.1 통합 기준 브랜치와 순서

정해야 할 내용:

- 어떤 브랜치를 통합 기준으로 사용할지
- 장소 API와 사용자 기능 중 무엇을 먼저 `main`에 병합할지
- `User` 모델과 마이그레이션을 언제 확정할지
- API 통합 작업을 누가 수행할지
- 통합 후 기존 브랜치를 삭제할지 유지할지

권장안:

1. `feature/be-connect-api`를 먼저 PR로 검토하고 `main`에 병합한다.
2. 사용자 인증 브랜치를 최신 `main` 기준으로 갱신한다.
3. 즐겨찾기 기능은 인증 기능 위에서 함께 검증한다.
4. 통합 브랜치에서 전체 테스트를 통과시킨 후 사용자 기능 PR을 병합한다.
5. 추천 기능은 통합된 `Place`와 `User` 모델을 기준으로 새 브랜치에서 개발한다.

통합할 때 특히 확인할 충돌:

- `config/settings.py`의 `INSTALLED_APPS`, `AUTH_USER_MODEL`, 환경 변수 설정
- `config/urls.py`의 `/api/` 라우팅
- `requirements.txt`의 `requests` 버전과 인증 관련 패키지
- 장소 API와 사용자 API의 성공·오류 응답 형식
- 기존 로컬 DB에서 커스텀 사용자 모델로 전환하는 방법

결정:

```text
통합 기준 브랜치:
첫 번째 PR:
통합 담당자:
통합 목표일:
기존 개발 DB 처리 방식:
```

### 2.2 백엔드 담당 범위

권장 역할 분담:

| 영역 | 권장 담당 | 완료 조건 |
| --- | --- | --- |
| 인증·사용자 | 사용자 기능 담당자 | 회원가입, 로그인, 토큰 갱신, 로그아웃 테스트 통과 |
| 프로필·선호 | 사용자 기능 담당자 | 내 정보 조회·수정과 선호 카테고리 저장 가능 |
| 즐겨찾기 | 사용자 기능 담당자 | 추가·조회·삭제와 중복 방지 테스트 통과 |
| 장소·혼잡도 | 장소 API 담당자 | 기존 수집·조회 API가 통합 후에도 동일하게 동작 |
| 추천·대체 장소 | 장소 API 담당자 | 조건에 맞는 3개 결과와 추천 이유 반환 |
| 통합 테스트 | 공동 | 인증 사용자와 비회원의 전체 흐름 검증 |

결정:

```text
인증·사용자 담당:
장소·혼잡도 담당:
추천 담당:
통합 테스트 담당:
```

### 2.3 공통 API 형식

현재 장소 API와 사용자 브랜치의 오류 응답 형식이 다르므로 하나로 통일한다.

권장안:

- JSON 필드는 `snake_case`를 사용한다.
- 날짜는 시간대가 포함된 ISO 8601 형식으로 반환한다.
- 정상 응답의 `message`는 안내가 없으면 빈 문자열로 둔다.
- 오류 응답의 `data`는 항상 `null`로 둔다.
- 입력 필드별 오류가 있으면 선택적으로 `errors`를 추가한다.
- 현재 일정에서는 `/api/v1`을 새로 만들지 않고 기존 `/api`를 유지한다.
- URL 끝의 슬래시는 현재 구현처럼 사용하지 않는다.

정상 응답 예시:

```json
{
  "success": true,
  "data": {
    "id": 1,
    "name": "경복궁"
  },
  "message": ""
}
```

오류 응답 예시:

```json
{
  "success": false,
  "data": null,
  "message": "latitude must be between -90 and 90.",
  "errors": {
    "latitude": ["유효한 위도를 입력해 주세요."]
  }
}
```

권장 HTTP 상태 코드:

| 상황 | 상태 코드 |
| --- | --- |
| 정상 조회·수정·삭제 | `200` |
| 생성 성공 | `201` |
| 요청 형식 또는 값 오류 | `400` |
| 인증 필요 또는 토큰 오류 | `401` |
| 접근 권한 없음 | `403` |
| 장소나 즐겨찾기 없음 | `404` |
| 중복 또는 상태 충돌 | `409` |
| 서버 내부 오류 | `500` |
| 외부 데이터 공급자 일시 장애 | `502` 또는 내부 배치 실패 기록 |

결정:

```text
JSON 필드 표기:
오류 data 값:
errors 필드 사용 여부:
API 버전 경로:
URL 슬래시 규칙:
```

### 2.4 인증과 비회원 사용 범위

정해야 할 내용:

- 장소 조회와 추천을 비회원에게도 제공할지
- access token과 refresh token 유효시간
- 계정당 한 세션만 허용할지
- Google 계정과 같은 이메일의 기존 계정을 어떻게 연결할지
- 탈퇴 사용자의 데이터 처리 방식

권장안:

- 장소 조회, 주변 장소, 추천, 대체 장소는 비회원도 사용할 수 있게 한다.
- JWT가 있으면 저장된 선호 카테고리와 이용 기록을 추천에 추가로 사용한다.
- 즐겨찾기와 프로필 수정은 인증 사용자만 허용한다.
- access token은 30분, refresh token은 14일을 시작값으로 사용한다.
- 초기 버전은 계정당 한 개의 refresh token만 유지한다.
- 실제 사용자 데이터가 쌓이기 전에 커스텀 `User` 모델을 확정한다.

권장 사용자 API:

```http
POST /api/auth/signup
POST /api/auth/login
POST /api/auth/google
POST /api/auth/refresh
POST /api/auth/logout
GET  /api/auth/nickname/random
GET  /api/users/me
PATCH /api/users/me
```

프로필 수정 요청 예시:

```http
PATCH /api/users/me
Authorization: Bearer {access_token}
Content-Type: application/json
```

```json
{
  "nickname": "산책좋아",
  "preferred_categories": ["tourist_attraction", "cultural_facility"],
  "preferred_crowd_levels": ["relaxed", "normal"]
}
```

결정:

```text
비회원 추천 허용 여부:
토큰 유효시간:
동시 로그인 정책:
프로필 수정 가능 필드:
계정 탈퇴 범위:
```

### 2.5 즐겨찾기 계약

현재 사용자 브랜치는 즐겨찾기 목록에서 `place_id`만 반환한다. 프런트가 장소마다
상세 API를 다시 호출하면 요청 수가 늘어나므로 카드 표시에 필요한 장소 정보를
함께 반환하는 것을 권장한다.

권장 API:

```http
GET    /api/favorites
POST   /api/favorites
DELETE /api/favorites/{place_id}
```

추가 요청 예시:

```json
{
  "place_id": 1
}
```

목록 응답 예시:

```json
{
  "success": true,
  "data": {
    "items": [
      {
        "place": {
          "id": 1,
          "name": "경복궁",
          "category": "관광지",
          "image_url": "https://example.com/image.jpg",
          "latest_crowd": {
            "level": "normal",
            "observed_at": "2026-09-05T14:10:00+09:00"
          }
        },
        "created_at": "2026-09-05T14:20:00+09:00"
      }
    ],
    "pagination": {
      "page": 1,
      "page_size": 20,
      "total": 1,
      "total_pages": 1
    }
  },
  "message": ""
}
```

결정:

```text
목록에서 장소 상세 포함 여부:
즐겨찾기 목록 페이지네이션 여부:
중복 추가 상태 코드:
비활성화된 장소 처리:
```

### 2.6 추천 API 입력 계약

추천은 조건이 많고 앞으로 확장될 가능성이 있으므로 `GET` 쿼리보다 `POST` JSON
요청을 권장한다. 조회 성격이지만 서버 상태를 변경하지는 않는다.

권장 API:

```http
POST /api/recommendations
```

현재 구현 가능한 1차 요청 예시:

```json
{
  "latitude": 37.576031,
  "longitude": 126.976722,
  "radius_km": 10,
  "stay_minutes": 120,
  "desired_crowd_levels": ["relaxed", "normal"],
  "categories": ["관광지", "문화시설"],
  "indoor_outdoor": "any",
  "companion": "couple",
  "limit": 3
}
```

입력 필드 권장 규칙:

| 필드 | 필수 | 권장 규칙 |
| --- | --- | --- |
| `latitude` | 예 | `-90` 이상 `90` 이하 |
| `longitude` | 예 | `-180` 이상 `180` 이하 |
| `radius_km` | 아니오 | 기본 `5`, 최대 `100` |
| `stay_minutes` | 아니오 | 양의 정수, 없으면 점수에서 제외 |
| `desired_crowd_levels` | 아니오 | 혼잡도 공통 enum 배열 |
| `categories` | 아니오 | 백엔드가 제공하는 카테고리 값 배열 |
| `indoor_outdoor` | 아니오 | `indoor`, `outdoor`, `any` |
| `companion` | 아니오 | `alone`, `friends`, `couple`, `family` |
| `limit` | 아니오 | 기본과 최대 모두 `3` 권장 |

이동 가능 시간을 입력받으려면 실제 이동시간 데이터가 필요하다. 현재 백엔드는
직선거리만 계산하므로 `travel_minutes`를 바로 제공한다고 약속하지 않는 것이 좋다.

권장 선택지:

1. 1차 버전은 `radius_km`만 사용하고 화면에도 거리로 표시한다.
2. 경로 API 공급자를 정한 뒤 `travel_mode`와 `travel_limit_minutes`를 추가한다.
3. 단순 거리 환산값을 사용한다면 반드시 `estimated_travel_minutes`로 표시한다.

결정:

```text
추천 API 메서드와 경로:
필수 입력값:
결과 개수:
이동시간 계산 방식:
companion을 실제 점수에 사용할지:
```

### 2.7 추천 점수 계약

초기에는 머신러닝보다 설명 가능한 가중치 기반 추천을 권장한다. 회의록의 제안을
시작값으로 사용하되 운영 데이터에 따라 설정으로 변경할 수 있게 한다.

권장 시작 가중치:

| 점수 항목 | 가중치 | 현재 사용 가능한 데이터 |
| --- | ---: | --- |
| 원하는 혼잡도와의 적합성 | 35 | 서울 일부 장소만 가능 |
| 사용자 취향 | 20 | 요청 카테고리, 저장된 선호 카테고리 |
| 거리 또는 이동시간 | 20 | 현재는 직선거리 가능 |
| 운영시간·날씨 등 상황 | 15 | 운영시간 일부, 날씨는 미구현 |
| 새로운 장소 발견 | 10 | 이용 기록이 없으면 제한적 |

점수 예시:

```text
최종 점수
= 혼잡도 적합성 × 0.35
+ 취향 적합성 × 0.20
+ 거리 적합성 × 0.20
+ 상황 적합성 × 0.15
+ 발견 점수 × 0.10
```

권장 결측값 처리:

- 혼잡도 값이 없으면 한산하다고 추정하지 않고 `unknown`으로 유지한다.
- 사용자가 혼잡도 정보가 있는 장소만 요청하면 `unknown` 장소는 제외한다.
- 선택 항목의 데이터가 없으면 해당 항목을 0점 처리하지 않고 사용 가능한 항목의
  가중치를 다시 정규화한다.
- 함께 반환하는 `score_confidence`는 실제로 계산에 사용된 가중치 합으로 정한다.
- 혼잡도가 핵심인 만큼 혼잡도 데이터가 없으면 신뢰도가 낮다는 사실을 추천
  이유와 응답에 표시한다.

예시:

```text
혼잡도 데이터 있음 + 취향 + 거리만 계산 가능
사용 가능한 가중치 합 = 35 + 20 + 20 = 75
score_confidence = 0.75
```

혼잡도 선호 점수 예시:

| 사용자 선호 | relaxed | normal | busy | crowded | unknown |
| --- | ---: | ---: | ---: | ---: | ---: |
| `relaxed` | 100 | 65 | 25 | 0 | 계산 제외 |
| `normal` | 70 | 100 | 65 | 20 | 계산 제외 |
| `busy` | 20 | 70 | 100 | 60 | 계산 제외 |
| `any` | 100 | 100 | 80 | 30 | 계산 제외 |

위 숫자는 확정값이 아니라 회의에서 조정할 시작안이다.

추천 결과의 동점 정렬 권장안:

1. 최종 점수가 높은 순서
2. 혼잡도 데이터가 더 최신인 순서
3. 거리가 가까운 순서
4. `Place.id`가 작은 순서

이렇게 마지막 기준까지 정해야 테스트 결과가 매번 동일하다.

결정:

```text
점수 항목:
가중치:
혼잡도 없는 장소 처리:
선택 항목 결측값 처리:
동점 정렬 기준:
점수와 이유를 응답에 공개할지:
```

### 2.8 추천 응답 계약

권장 응답 예시:

```json
{
  "success": true,
  "data": {
    "items": [
      {
        "rank": 1,
        "place": {
          "id": 1,
          "name": "경복궁",
          "category": "관광지",
          "address": "서울특별시 종로구 사직로 161",
          "latitude": 37.576031,
          "longitude": 126.976722,
          "image_url": "https://example.com/image.jpg"
        },
        "distance_km": 1.8,
        "recommendation_score": 86.4,
        "score_confidence": 0.75,
        "score_breakdown": {
          "crowd_fit": 33.0,
          "preference_fit": 18.0,
          "distance_fit": 14.0,
          "context_fit": null,
          "discovery": null
        },
        "reasons": [
          "원하는 혼잡도와 일치해요.",
          "선호한 관광지 카테고리예요.",
          "현재 위치에서 1.8km 떨어져 있어요."
        ],
        "crowd": {
          "type": "realtime",
          "level": "normal",
          "source": "seoul_realtime",
          "observed_at": "2026-09-05T14:10:00+09:00",
          "is_stale": false,
          "confidence": null
        }
      }
    ],
    "generated_at": "2026-09-05T14:20:00+09:00"
  },
  "message": ""
}
```

권장 사항:

- 결과가 3개보다 적으면 가능한 항목만 반환하고 이유를 `message`에 설명한다.
- 추천 이유는 프런트가 조합하지 않고 백엔드가 완성된 문장으로 반환한다.
- 점수 항목이 계산되지 않았다면 `0`이 아니라 `null`을 반환한다.
- 외부 API 원본 데이터나 내부 계산용 상세 정보는 응답하지 않는다.

결정:

```text
점수 공개 여부:
항목별 점수 공개 여부:
추천 이유 생성 위치:
3개 미만일 때 처리:
```

### 2.9 혼잡한 장소의 대체 장소 계약

권장 API:

```http
GET /api/places/{place_id}/alternatives?latitude=37.57&longitude=126.98&radius_km=10&limit=3
```

권장 후보 조건:

1. 원래 장소와 카테고리 또는 태그가 유사하다.
2. 원래 장소 또는 사용자 위치에서 지정 반경 안에 있다.
3. 원래 장소보다 혼잡도가 낮거나 사용자가 원하는 혼잡도에 가깝다.
4. 비활성화된 장소는 제외한다.
5. 운영 여부를 알 수 있고 닫혀 있다면 제외한다.

권장 구현 구조:

- 별도의 중복 알고리즘을 만들지 않는다.
- 하나의 `RecommendationService`에 `reference_place_id`를 전달해 같은 점수 계산을
  재사용한다.
- 카테고리 일치 점수는 일반 추천보다 대체 장소 추천에서 더 높게 반영한다.

대체 장소가 없는 응답 예시:

```json
{
  "success": true,
  "data": {
    "items": [],
    "reference_place_id": 1
  },
  "message": "현재 조건에 맞는 대체 장소를 찾지 못했습니다."
}
```

결정:

```text
원래 장소와의 유사성 기준:
거리 기준점은 사용자 위치인지 원래 장소인지:
대체 장소를 제시할 원래 장소 혼잡도 기준:
혼잡도 없는 대체 후보 처리:
```

### 2.10 혼잡도 종류와 최신성 계약

전국 모든 장소에 같은 수준의 실시간 데이터가 없으므로 데이터의 성격을 숨기지
않고 구분한다.

권장 enum:

| 값 | 의미 |
| --- | --- |
| `realtime` | 지자체 또는 통신사 실시간 관측 |
| `forecast` | 특정 미래 시각에 대한 예상값 |
| `community` | 사용자 현장 제보를 집계한 값 |
| `unknown` | 사용할 수 있는 혼잡도 없음 |

권장 혼잡도 단계:

```text
relaxed
normal
busy
crowded
unknown
```

프런트의 `low`, `medium`, `high`와 백엔드의 현재 값을 섞지 말고 위 enum 하나를
공통 계약으로 사용하는 것을 권장한다.

권장 최신성 규칙:

- 서울 실시간 혼잡도는 15분 주기 수집을 목표로 한다.
- 관측 후 30분이 지나면 `is_stale=true`로 표시하는 것을 시작안으로 한다.
- 오래된 값을 최신 혼잡도 필터나 추천 점수에 사용할지 회의에서 정한다.
- 응답에는 항상 `source`, `observed_at`, `is_stale`를 포함한다.
- 예측 데이터는 관측 시각과 예측 대상 시각을 분리한다.

예측 응답 예시:

```json
{
  "type": "forecast",
  "level": "busy",
  "source": "provider_to_be_decided",
  "generated_at": "2026-09-05T12:00:00+09:00",
  "target_at": "2026-09-05T17:00:00+09:00",
  "confidence": 0.78,
  "is_stale": false
}
```

데이터 모델 권장안:

- 현재 관측값은 기존 `CrowdData`에 유지한다.
- 미래 예측값은 의미가 다르므로 `CrowdForecast`로 분리한다.
- 사용자 제보를 구현할 때는 사용자, 위치 검증, 제보 시각이 필요한
  `CommunityCrowdReport`로 분리한다.
- API 응답 단계에서 세 데이터 유형을 공통 혼잡도 형식으로 정규화한다.

결정:

```text
혼잡도 enum:
실시간 stale 기준:
오래된 값을 추천에 사용할지:
예측 데이터 공급자:
CrowdForecast 분리 여부:
사용자 제보는 이번 범위에 포함할지:
```

### 2.11 시간대별 혼잡도 계약

권장 API:

```http
GET /api/places/{place_id}/crowd/timeline?from=2026-09-05T14:00:00%2B09:00&to=2026-09-05T20:00:00%2B09:00
```

권장 응답 예시:

```json
{
  "success": true,
  "data": {
    "place_id": 1,
    "items": [
      {
        "type": "realtime",
        "level": "normal",
        "observed_at": "2026-09-05T14:00:00+09:00",
        "target_at": "2026-09-05T14:00:00+09:00",
        "source": "seoul_realtime"
      },
      {
        "type": "forecast",
        "level": "relaxed",
        "generated_at": "2026-09-05T12:00:00+09:00",
        "target_at": "2026-09-05T18:00:00+09:00",
        "source": "provider_to_be_decided"
      }
    ],
    "recommended_visit_at": "2026-09-05T18:00:00+09:00"
  },
  "message": ""
}
```

권장 원칙:

- 예측 공급자가 확정되지 않았다면 예측값을 임의로 만들지 않는다.
- 과거 평균을 사용하면 `forecast`가 아니라 `historical_average`처럼 별도로 표시한다.
- 데이터가 없으면 빈 배열과 설명 메시지를 반환한다.

결정:

```text
시간대별 비교를 1차 출시 범위에 넣을지:
예측 공급자와 호출 한도:
과거 평균 fallback 사용 여부:
recommended_visit_at 계산 기준:
```

### 2.12 카테고리 계약

현재 TourAPI 카테고리명과 프런트 목 데이터의 카테고리명이 다르다. 화면 문구와
검색 조건이 바뀌어도 저장 데이터가 흔들리지 않도록 코드와 표시 이름을 분리하는
것을 권장한다.

예시:

```json
{
  "code": "cultural_facility",
  "label": "문화시설",
  "source_code": "14"
}
```

권장 API:

```http
GET /api/categories
```

응답 예시:

```json
{
  "success": true,
  "data": {
    "items": [
      {"code": "tourist_attraction", "label": "관광지"},
      {"code": "cultural_facility", "label": "문화시설"},
      {"code": "festival", "label": "축제·공연·행사"},
      {"code": "leisure", "label": "레포츠"},
      {"code": "shopping", "label": "쇼핑"},
      {"code": "restaurant", "label": "음식점"}
    ]
  },
  "message": ""
}
```

결정:

```text
저장용 카테고리 코드:
프런트 표시 이름:
복수 카테고리 허용 여부:
categories API 제공 여부:
```

### 2.13 추천 행동 기록과 개인정보

향후 추천을 개선하려면 결과만 계산하지 말고 사용자의 반응을 기록해야 한다.
다만 이번 MVP에서는 최소한의 이벤트만 수집한다.

권장 이벤트:

```text
recommendation_impression
place_click
favorite_add
favorite_remove
recommendation_like
recommendation_dislike
```

권장 API:

```http
POST /api/recommendation-events
```

요청 예시:

```json
{
  "event_type": "place_click",
  "place_id": 1,
  "recommendation_request_id": "c436c79f-25d7-4e27-9f30-4c891e08985d"
}
```

권장 원칙:

- 정확한 이동 경로를 계속 저장하지 않는다.
- 추천 요청에 필요한 위치는 기본적으로 계산 후 폐기한다.
- 위치 이력을 저장해야 한다면 목적, 기간, 동의 방식을 먼저 정한다.
- 로그에 access token, refresh token, API 키, 사용자 원본 위치를 남기지 않는다.

결정:

```text
이번 버전에 기록할 이벤트:
비회원 이벤트 식별 방식:
사용자 위치 저장 여부:
이벤트 보존기간:
```

### 2.14 리워드 최소 범위

최종 MVP 방향에서는 장소 저장이 필수이고 커뮤니티·리뷰는 후속 기능이다. 하지만
서비스 소개에서 리워드를 핵심으로 유지한다면 보여줄 수 있는 최소 기능을 정해야
한다.

권장 최소안:

- 추천받은 대체 장소를 저장하거나 방문 확인했을 때 포인트 적립
- 포인트 원장 `PointTransaction`으로 적립 근거와 중복 여부 기록
- 현재 포인트 조회
- 현금 출금, 광고 수익 배분, 상품권 교환은 구현하지 않음

예시 API:

```http
GET  /api/rewards/balance
GET  /api/rewards/transactions
POST /api/rewards/visit-claims
```

리워드를 이번 범위에서 제외한다면 서비스 소개도 “리워드 플랫폼”보다 “혼잡도
기반 장소 추천 서비스”에 맞게 수정하는 것을 권장한다.

결정:

```text
리워드 포함 여부:
포인트 적립 조건:
방문 확인 방식:
중복 적립 제한:
교환·출금은 제외하는지:
```

### 2.15 배치 작업과 운영 계약

현재 외부 데이터 수집은 관리 명령을 사람이 실행한다. 실제 서비스에서는 수집
주기와 장애 처리를 정해야 한다.

권장 시작안:

| 작업 | 주기 | 실패 처리 |
| --- | --- | --- |
| 서울 실시간 혼잡도 | 15분 | 영역별 재시도 후 실패 기록 |
| TourAPI 변경분 | 하루 1회 | 다음 실행에서 다시 처리 |
| TourAPI 전체 목록 대조 | 주 1회 | 호출 한도 확인 후 실행 |
| 오래된 혼잡 관측 정리 | 하루 1회 | 우선 30일 보존 |

정해야 할 내용:

- cron, 플랫폼 스케줄러, Celery 중 무엇을 사용할지
- 배치 실행 결과와 마지막 성공 시각을 어디에 저장할지
- 부분 성공을 성공으로 볼지 실패로 볼지
- API별 호출 한도와 초과 시 정책
- 알림을 받을 담당자와 채널

권장안:

- 첫 배포에서는 관리 명령과 플랫폼 스케줄러 또는 cron을 사용한다.
- 작업량이 커지거나 비동기 사용자 작업이 생길 때 Celery를 도입한다.
- 각 작업의 `started_at`, `finished_at`, `status`, `processed`, `failed`,
  `error_summary`를 기록한다.
- `/healthz`와 별도로 운영 점검용 마지막 동기화 상태를 제공하되 관리자만
  접근할 수 있게 한다.

결정:

```text
스케줄러:
서울 혼잡도 주기:
TourAPI 주기:
관측 보존기간:
배치 실패 알림 담당:
```

### 2.16 프런트엔드 연결을 위한 보안 계약

권장안:

- 외부 API 키는 백엔드 환경 변수에만 두고 Flutter 앱에는 넣지 않는다.
- 개발·운영별 허용 origin을 환경 변수로 관리한다.
- Flutter Web을 사용하면 CORS 허용 목록을 명시한다.
- `DEBUG=false`인 환경에서는 안전하지 않은 기본 `SECRET_KEY`로 실행되지 않게 한다.
- 인증이 필요 없는 API에도 운영 환경에서는 요청 제한을 검토한다.

환경 변수 이름 예시:

```dotenv
DJANGO_SECRET_KEY=
DJANGO_DEBUG=false
DJANGO_ALLOWED_HOSTS=api.example.com
DJANGO_CORS_ALLOWED_ORIGINS=https://app.example.com
GOOGLE_CLIENT_ID=
TOUR_API_SERVICE_KEY=
SEOUL_OPEN_API_KEY=
```

결정:

```text
프런트 개발 origin:
운영 origin:
공용 개발 서버 주소:
요청 제한 적용 여부:
```

## 3 권장 개발 순서

### 1단계 백엔드 회의 직후

1. 장소 API 브랜치 PR 생성 및 병합
2. 사용자 인증·즐겨찾기 브랜치 갱신 및 통합
3. 공통 응답 형식, enum, URL 확정
4. `GET/PATCH /api/users/me`와 선호 카테고리 구현
5. 통합 테스트 작성

완료 기준:

- 장소 API 기존 테스트가 계속 통과한다.
- 인증·즐겨찾기 테스트가 통과한다.
- 인증 사용자와 비회원의 허용 범위가 테스트로 고정된다.
- 프런트가 사용할 API 문서와 예시 응답이 준비된다.

### 2단계 핵심 추천 기능

1. 공통 `RecommendationService` 구현
2. `POST /api/recommendations` 구현
3. 점수 항목, 결측값, 동점 정렬 테스트
4. 추천 이유 생성
5. `GET /api/places/{place_id}/alternatives` 구현

완료 기준:

- 같은 데이터와 조건에서 결과 순서가 항상 동일하다.
- 최대 3개 결과를 반환한다.
- 혼잡도 없는 장소를 한산하다고 표시하지 않는다.
- 각 결과에 점수, 이유, 데이터 기준 시각이 있다.
- 비활성화된 장소는 추천하지 않는다.

### 3단계 시간대와 운영 기능

1. 예측 데이터 공급자 검증
2. `CrowdForecast` 모델과 동기화 구현 여부 결정
3. 시간대별 혼잡도 API 구현
4. 실시간 혼잡도 stale 판정
5. 자동 배치와 실패 기록 구현

완료 기준:

- 실시간, 예측, 과거 평균을 구분한다.
- 오래된 값을 최신 데이터처럼 노출하지 않는다.
- 배치 실패가 조용히 사라지지 않고 확인 가능하다.

### 4단계 프런트엔드 통합

1. 카테고리와 혼잡도 enum 공유
2. 개발 서버 CORS와 접속 주소 설정
3. 로그인, 장소 검색, 추천, 즐겨찾기 순서로 연결
4. 빈 결과, 오래된 데이터, 인증 만료, 서버 오류 화면 검증
5. 실제 기기와 Flutter Web에서 통합 테스트

## 4 두 번째 전체 회의에서 공유할 내용

프런트 팀과의 회의에서는 구현 세부사항보다 다음 계약을 공유한다.

- 사용 가능한 API 목록과 아직 개발 중인 API
- 요청 필드와 필수 여부
- 카테고리와 혼잡도 enum
- 인증이 필요한 API
- 추천 결과가 항상 3개인지, 부족할 수 있는지
- 추천 이유와 데이터 기준 시각의 표시 방법
- 실시간, 예측, 정보 없음의 UI 구분
- Flutter Web CORS와 실제 기기 접속 주소
- 오류와 빈 결과 응답 예시
- 백엔드가 준비할 날짜와 프런트 연결 담당자

권장 시연 순서:

```text
1. 비회원으로 현재 위치 주변 장소 조회
2. 조건을 입력해 장소 3개 추천
3. 혼잡한 장소를 선택해 대체 장소 확인
4. 로그인 후 장소 저장
5. 저장 목록에서 최신 혼잡도 확인
6. 데이터 출처와 기준 시각 확인
```

## 5 회의 종료 체크리스트

### 범위

- [ ] 1차 출시 필수 기능을 확정했다.
- [ ] 리워드 포함 여부와 최소 범위를 확정했다.
- [ ] 커뮤니티, 리뷰, 코스 생성 등 제외 범위를 확정했다.

### 통합

- [ ] 통합 기준 브랜치와 PR 순서를 정했다.
- [ ] 사용자 모델과 기존 개발 DB 처리 방식을 정했다.
- [ ] 설정, URL, 의존성 충돌 담당자를 정했다.

### API

- [ ] 공통 성공·오류 응답을 정했다.
- [ ] 인증 필요 범위를 정했다.
- [ ] 추천 요청과 응답 형식을 정했다.
- [ ] 대체 장소 API 형식을 정했다.
- [ ] 즐겨찾기 응답에 포함할 장소 정보를 정했다.
- [ ] 카테고리와 혼잡도 enum을 정했다.

### 추천

- [ ] 점수 항목과 가중치를 정했다.
- [ ] 혼잡도 없는 장소의 처리 방식을 정했다.
- [ ] 결측값과 동점 정렬 방식을 정했다.
- [ ] 이동시간 또는 거리 계산 방식을 정했다.
- [ ] 추천 이유를 백엔드에서 생성할지 정했다.

### 혼잡도와 운영

- [ ] 실시간 데이터의 stale 기준을 정했다.
- [ ] 예측 데이터 공급자와 시간대별 기능 범위를 정했다.
- [ ] 수집 주기와 스케줄러를 정했다.
- [ ] 실패 기록과 알림 담당자를 정했다.

### 일정과 담당

- [ ] 각 기능 담당자를 정했다.
- [ ] 백엔드 통합 완료일을 정했다.
- [ ] 추천 API 완료일을 정했다.
- [ ] 프런트 연결 가능일을 정했다.
- [ ] 통합 테스트 담당자와 완료 조건을 정했다.

## 6 회의 결정 기록 양식

회의 중 아래 표를 채우고 회의가 끝난 뒤 저장소의 결정 기록 문서에 옮긴다.

| 번호 | 결정 항목 | 최종 결정 | 담당자 | 완료일 | 비고 |
| ---: | --- | --- | --- | --- | --- |
| 1 | 통합 순서 |  |  |  |  |
| 2 | 인증·비회원 범위 |  |  |  |  |
| 3 | 공통 API 응답 |  |  |  |  |
| 4 | 추천 입력값 |  |  |  |  |
| 5 | 추천 가중치 |  |  |  |  |
| 6 | 결측값 처리 |  |  |  |  |
| 7 | 대체 장소 기준 |  |  |  |  |
| 8 | 혼잡도 enum과 stale 기준 |  |  |  |  |
| 9 | 예측 데이터 |  |  |  |  |
| 10 | 리워드 범위 |  |  |  |  |
| 11 | 배치 주기 |  |  |  |  |
| 12 | 프런트 연결일 |  |  |  |  |

## 7 최종 권장안 요약

시간이 부족할 때는 다음 범위를 우선한다.

```text
필수
- 장소 API와 사용자 인증·즐겨찾기 통합
- 비회원도 가능한 조건 기반 장소 3개 추천
- 혼잡한 목적지의 대체 장소 3개 추천
- 추천 이유, 혼잡도 출처, 데이터 기준 시각 표시
- 혼잡도 자동 갱신과 오래된 데이터 구분
- Flutter가 사용할 공통 API 계약

데이터 공급자가 확정되면 추가
- 시간대별 예상 혼잡도

후속
- 사용자 현장 제보
- 알림
- 코스 생성
- 커뮤니티와 사진 리뷰
- 포인트 교환과 출금
- 머신러닝 개인화
```

추천 알고리즘은 처음부터 복잡한 AI로 만들지 않는다. 현재 보유한 장소, 거리,
혼잡도, 사용자 선호 데이터로 설명 가능한 가중치 기반 추천을 완성하고, 클릭과
저장 등 실제 행동 데이터가 쌓인 뒤 개인화 방식을 확장한다.
