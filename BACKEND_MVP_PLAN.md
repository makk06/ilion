# 전국 추천 MVP 최종 개발 계획

> 2026-09-13 구현 정책 갱신: 이 문서는 2026-09-12의 최초 범위 합의 기록이다. 실제 추천 점수·응답 계약은 [추천 MVP 계약](tourist_congestion_backend/docs/recommendation-mvp.md)과 [추천 품질 평가](tourist_congestion_backend/docs/recommendation-quality-2026-09-13.md)를 우선한다. 기본 추천은 날씨를 고려하고, 명시 선호가 없으면 약한 관광 탐색 카테고리 점수를 사용한다. 결측을 후보별로 재정규화하던 초기 계획은 전 후보 공통 분모·중립 순위 기준으로 대체했다. 서울 혼잡 관측의 30~45분 지연 반영·45분 만료는 이번 점수 변경에 앞선 선행 변경으로, 현재 계약의 시험 기준이다. 아래의 초기 문구와 충돌하면 최신 계약을 따른다.

> 2026-09-13 수집 권한 갱신: 사용자는 활성 작업 중 검증에 필요한 제한 범위의 수집 워커를 담당자가 필요할 때 재승인 없이 실행하도록 허용했다. 범위·기존 대기 작업·예산을 먼저 확인하고 유한 `--max-jobs`로 실행하며 호출량과 결과를 보고한다. 이는 상주 24시간 워커, 무인 정기 스케줄, 전면 수집, 배포를 약속하거나 활성화하는 권한이 아니다. 이번 7도시 35격자·서울 12영역의 총 82회 실제 호출 및 현재 시간대 검증은 [추천 품질 평가](tourist_congestion_backend/docs/recommendation-quality-2026-09-13.md)에 기록했다.

- 정리일: 2026-09-12
- 목적: 사용자와 합의한 범위 및 수집 계약을 구현 담당 Sol에게 인계한다.
- 이 문서가 기존 `BACKEND_RECOMMENDATION_PLAN.md`와 충돌하는 제품 범위의 최신 기준이다. 과거 회의 문서는 당시 기록으로 유지한다.
- 현재 작업은 계획 저장까지이며 애플리케이션 구현은 Sol이 수행한다.

## 1. 확정한 방향

- 전국 장소 검색·상세·추천을 이번 MVP부터 지원한다. 서울 전체 완성을 전국화의 선행 조건으로 두지 않는다.
- 추천은 기본 최대 10개다. 후보 부족 시 실제 개수와 사유를 반환하고 반경·필수 조건을 자동 완화하지 않는다.
- 날씨와 실내외 적합도를 이번 MVP의 핵심 추천에 포함한다.
- 서울 일부 검증된 매핑 장소는 실시간 혼잡도를 사용한다. 전국 실시간 혼잡도를 보장하지 않는다.
- 선택한 장소와 관련된 장소 및 대체 장소를 추천하고 거리를 고려한다.
- 사용자 피드백은 초기 추천 점수에 반영하지 않는다.
- 로컬 DB는 SQLite다. 운영 DB는 미정이며 PostgreSQL 등으로 임의 확정하지 않는다.
- TMAP은 사용자가 신청할 예정이다. 승인을 기다리지 않고 거리 기반 추천을 구현한다.

## 2. 기존 기반과 MVP 기능

기존 `places`의 장소·상세·출처·혼잡 영역·관측 모델, 수집 명령과 `users`의 인증·즐겨찾기를 재사용한다. 구현 시작 시 실제 코드와 테스트 상태를 다시 확인한다.

### 기본 추천

- 제안 경로: `POST /api/recommendations`.
- 입력: 위치, 반경, 카테고리, 희망 혼잡도, 실내외, 방문 예정 시각, 결과 수. 결과 수 기본값·상한은 10으로 시작한다.
- 방문 시각이 없으면 현재 시각을 기준으로 한다. 미래 방문 요청에 현재 혼잡도를 미래 관측처럼 적용하지 않는다.
- 요청 카테고리를 우선하고 없으면 로그인 사용자의 저장 선호를 적용한다. 비회원도 추천 가능하다.
- 거리·선호·혼잡도·날씨 적합도로 시작하고 가중치와 기준은 설정으로 분리한다. 기존 35/20/20/15/10은 확정값이 아니다.
- 유효하지 않은 데이터의 점수는 제외하고 사용 가능한 비중으로 재정규화한다.
- 추천 이유, 점수 상세, 사용 가능한 데이터 비중, 누락 항목, 출처·기준 시각을 반환한다. 데이터 비중을 통계적 정확도로 표현하지 않는다.
- 동일 데이터·동일 입력에서 정렬은 결정적이어야 한다.
- 거리와 이동시간을 분리한다. TMAP 전에는 직선거리임을 표시하고 이동시간은 null로 둔다.

### 날씨와 실내외

- 단기예보에서 방문 시각에 적합한 최신 발표본을 선택한다.
- 비·눈·강풍·고온·저온에 따른 야외 적합도를 조정한다. 임계값·감점은 근거와 함께 설정으로 관리한다.
- 실내외는 실내·야외·혼합·미확인으로 확장하고 분류 출처·근거를 보존한다.
- 기존 수집은 `unknown`을 저장하므로 실제 분류 보강이 필수다. 상세정보 및 검토된 규칙으로 명확한 후보부터 보강하며 카테고리만으로 일괄 단정하지 않는다.
- 날씨 또는 실내외가 미확인이면 해당 적합도를 추측하지 않는다. 혼합 장소도 무조건 실내처럼 취급하지 않는다.
- 야외 필수 조건을 실내 추천으로 몰래 바꾸지 않는다. 필요한 날씨 안내를 제공한다.

### 관련 장소 및 사용자 기능

- 제안 경로: `GET /api/places/{place_id}/alternatives`.
- 기준 장소 제외, 연관 관광지 관계 우선 활용, 관계가 없으면 카테고리·거리로 후보 선정한다.
- 사용 가능한 날씨·혼잡도를 함께 고려한다. 거리 기준점은 사용자 위치가 있으면 사용자 위치, 없으면 선택 장소라는 초기안을 명세에 명시한다.
- 사용자 선호 조회·수정, 기존 즐겨찾기 연동을 보완한다.
- 추천 요청 ID·결과·순위·알고리즘 버전과 이후 행동을 연결할 구조를 마련한다.
- 피드백 생성·본인 조회를 구현하되 타입별 의미 등 미확정 계약은 별도로 확인한다.
- 비회원 식별, 실제 이벤트 종류, 위치 저장·보존기간은 미확정이다. 구조 구현과 실제 수집 활성화를 분리하고 정책 확정 전 임의로 수집하지 않는다.

## 3. 확보 데이터와 적용 우선순위

사용자가 신청·승인을 알린 목록이다. 실제 인증키 사용 가능 여부, 명세, 한도, 대상 범위는 별도 확인해야 한다.

- 기상청: 단기예보 조회, 단기예보 통보문, 중기예보, 기상특보, 예보구역정보, ASOS 시간자료.
- 한국관광공사: 관광지별 연관 관광지, 관광지 집중률 방문자 추이 예측, 지역별 관광 자원 수요, 지역별 관광 수요 강도, 두루누비, 지역별 방문자수.
- 기존 연동: TourAPI 국문 관광정보, 서울 실시간 인구·혼잡도.

이번 핵심 연동은 기존 장소·혼잡도와 단기예보, 연관 관광지다. 방문 집중 예측은 제공 범위·시간 해상도를 검증해 보강한다. 지역 통계 분석, 두루누비 코스, 중기예보·ASOS 등의 확대 사용은 출시 필수 작업에서 제외한다.

전국 대상 카테고리는 기존 잠정안인 관광지·문화시설·행사·레포츠·쇼핑·음식점부터 명세를 확인하여 설정한다. 숙박·여행코스의 자동 포함은 하지 않는다.

## 4. 수집과 추천의 계약

정기 작업과 요청 시 보충 작업은 같은 공급자 클라이언트·정규화·검증·저장 서비스를 사용한다. 추천 점수 계산기가 외부 API 클라이언트에 직접 의존하지 않는다.

흐름: 스케줄러 또는 보충 요청 → 중복 방지된 수집 작업 → 워커 → 외부 API → 검증·DB 저장 → 추천에서 정규화된 데이터 조회.

### 초기 예약안

아래 시각은 한국시간이다. 실제 공급자 발표 시각·갱신 주기·승인 한도 확인 전에는 운영 확정값이 아니다.

| 데이터 | 초기 실행안 | 범위 |
| --- | --- | --- |
| 전국 장소 목록 | 매일 03:00 변경 확인, 일요일 04:00 전체 대조 | 페이지 단위 이어받기, 변경 조회 지원 여부 확인, 중복 실행 방지 |
| 장소 상세 | 매일 04:30 | 조회·추천 대상 중 누락·오래된 상세 우선, 호출 예산 내 처리 |
| 서울 혼잡도 | 매시 00·15·30·45분 | 2026-09-13부터 공식 121개 영역 수집, 추천 연결은 검증된 장소 매핑만 |
| 단기예보 | 발표본 제공 일정에 맞춰 발표 후 10분부터 | 요청 지역 격자, 자주 요청되는 격자는 정기 수집 |
| 방문 집중 예측 | 매일 06:00 | 제공 범위·호출량에 따라 전체 또는 우선 대상 |
| 연관 관광지 | 월요일 05:00 | 전체 또는 우선 대상, 부분 실패로 관계 삭제 금지 |
| 지역 통계 | 후속 활성화 시 매일 06:30 새 자료 확인 | 실제 집계 기간 유지 |
| 작업 상태 점검 | 5분마다 | 실패·지연·만료 확인, 외부 호출 없음 |

날씨는 장소별이 아니라 격자별 호출을 공유한다. 서울 121개 영역을 15분마다 호출하면 정기 호출만 하루 11,616회다. 실제 지연·사용률과 저장량을 확인한 뒤 상주 수집 주기를 확정한다.

### 보충 수집과 호출 예산

- 후보를 먼저 좁히고 필요한 영역·격자만 갱신한다. 요청마다 전국을 수집하지 않는다.
- 동일 대상의 진행 작업을 공유하고 혼잡도는 동일 영역 5분 이내 재호출을 억제하는 안으로 시작한다.
- 같은 날씨 격자·발표본을 성공적으로 저장했다면 중복 호출하지 않는다.
- 추천 요청 전체에서 보충 수집 대기는 최대 2초다. HTTP 처리가 끝나도 워커 작업은 계속한다.
- 공급자별 실제 요청 예산을 정기 70%, 보충·재시도 20%, 예비 10%로 시작한다. 페이지·재시도도 호출에 포함한다.
- 기본 재시도 최대 2회, 간격 증가 적용. 인증 오류는 즉시 중단, 한도 초과는 공급자 안내·초기화 시점을 따른다.
- 신규 발표본의 제공 지연, 작업 실패, 외부 장애를 구분한다.

### 유효성과 결측

- 서울 현재 혼잡도(45분 시험 기준): 관측 후 15분 이내 신선, 15~30분은 기준 시각을 표시하고 정상 비중으로 사용한다. 30~45분은 지연 관측으로 표시하고 혼잡 점수 가중치를 절반으로 낮춘다. 이 구간은 `quiet_required`의 확실한 한산함 근거로 쓰지 않는다. 45분 초과는 현재 점수에서 제외한다. 공급자 지연과 추천 품질을 관측한 뒤 기준을 재검토한다.
- 날씨: 방문 시각을 포함하는 예보 중 최신 발표본을 사용한다. 발표 누락 허용 지연은 공급자 명세에 맞춰 따로 설정한다. 혼잡도와 같은 30분 규칙을 적용하지 않는다.
- `fetched_at`이 갱신돼도 원래 관측·예측의 유효기간이 늘어나지 않는다.
- 미래 방문 집중은 실제 제공 날짜·기간의 예상 정보로만 쓴다. 변환 기준이 검증되기 전 현재 혼잡도 척도에 합치지 않는다.
- 지역 통계는 개별 장소의 현재 혼잡도로 사용하지 않는다.
- '한적함 선호'는 미확인 장소를 다른 근거로 추천 가능, '한적함 확인 필수'는 미확인 장소 제외.
- '평소보다 한적', '날씨에 적합' 등은 실제 비교·판단 근거가 있을 때만 추천 이유에 사용한다.

## 5. 저장 및 실행 구조

- `places`, `users` 재사용. `recommendations`, 날씨, 관광 관계·예측 영역은 책임에 따라 분리하되 불필요한 범용 프레임워크는 만들지 않는다.
- 장소·상세는 최신 상태 갱신, 혼잡도는 영역·관측 시각별 이력, 날씨는 공급자·격자·발표 시각·대상 시각별 저장.
- 방문 예측은 공급자·대상·발표 버전·대상 기간, 지역 통계는 공급자·지역·지표·집계 기간, 관계는 공급자·기준 장소·연관 장소로 중복을 방지한다. 실제 응답에 없는 식별자를 가정하지 않는다.
- 출처·외부 ID·단위·공간 범위·관측/발표/대상/수집 시각·매칭 상태·원본을 보존한다. 원본에 인증정보가 포함되지 않게 한다.
- 내부 `Place.id`와 외부 식별자는 분리한다. 매칭 불확실 자료는 검토 대상으로 남긴다.
- 실패·빈 응답이 정상 데이터를 덮어쓰지 않게 하고, 완전한 갱신 단위를 트랜잭션으로 반영한다.
- 시각은 UTC 저장, 예약·표시는 한국시간으로 한다.
- 초기 보관 제안: 혼잡도 및 지난 예보 30일, 작업 로그 90일, 지역 통계 유지. 자동 삭제는 정책 확정 후 활성화한다. 사용자 기록은 별도 정책이다.
- DB 작업 테이블과 별도 Django 워커로 시작한다. 중복 방지 키·재시도·실행 제한시간·재시작 후 복구를 갖춘다. 이후 큐 교체 시 수집·추천 서비스는 재사용한다.
- SQLite 개발에서는 워커 1개, 외부 통신은 쓰기 트랜잭션 밖에서 수행하고 짧게 저장한다.
- TMAP은 별도 경로 인터페이스로 붙일 수 있게 한다. 승인 전 실제 연동이나 이동시간 추측은 하지 않는다.

## 6. 개발 중 데이터 구성

- 전국 기본 장소는 실제 적재 가능하게 한다. 호출 예상량을 먼저 계산하고 중단·재개와 범위 제한을 지원한다.
- 상세·날씨·관계는 호출량을 고려해 필요한 대상부터 채운다. 서울 후보만 수집하던 이전 제안은 전국 기본 목록 수집으로 대체한다.
- 기존 `seed_dev_data`를 확장해 API 키 없는 고정 샘플을 제공한다.
- 제안 명령 `sync_mvp_data`로 최초·수동 수집, `run_data_worker --dev`로 개발 중 정기·보충 수집을 수행한다. 실제 명칭은 기존 명령과 조화롭게 정한다.
- 웹 서버 실행 자체가 전체 수집을 유발하지 않게 한다.
- 실제 키와 SQLite DB는 Git에 올리지 않는다. 자동 테스트는 고정 시각·샘플 응답을 사용한다. 키가 없으면 검증된 명세·fixture로 진행하고 실호출 검증 미완료를 명시한다.

## 7. 구현 순서와 완료 기준

1. 기존 코드·테스트·규칙 확인, 공급자별 공식 명세·샘플·한도·매핑 검증. 미확인 계약을 명시한다.
2. 전국 장소 적재와 작업 관리·최신성·중복 방지, 날씨 모델·수집 및 실내외 보강 기반.
3. 최대 10개 핵심 추천, 선호·거리·날씨·서울 혼잡도와 결측·필수 조건 처리.
4. 관련·대체 장소 추천, 제공 범위 내 방문 집중 예측 연결, 사용자 선호·행동·피드백 보완.
5. API 명세·개발 실행 안내·프런트 인계, 정상 및 장애 상황 통합 검증.

완료 검증에는 전국 서울 외 사례, 날씨로 순위가 달라지는 근거 있는 실내외 사례, 미래 방문과 현재 관측 구분, 혼잡도 만료, 후보 부족, 동점 정렬, 중복 작업, 재시도·한도·부분 실패, SQLite 재시작 복구, 기존 인증·장소 회귀를 포함한다. 프런트 수정이 필요한 통합 범위는 별도로 확인하며 미연동 상태를 완료로 표현하지 않는다.

현재 브랜치의 1차 구현 단위는 위 1~3 단계로 잡는다. 후속 범위는 전체 목표로 유지하되 기존 브랜치·PR 규칙을 존중하고 자동 병합·배포·푸시는 하지 않는다.

## 8. 후속 및 미확정

- 후속: TMAP 이동시간, 실시간 혼잡도 공급 지역 확대, 지역 통계 기반 고도화, 두루누비 코스, 학습형 개인화, 리워드·커뮤니티.
- 미확정: 운영 DB·서버, 실제 API 한도·제공 시각·시간 해상도, 점수 가중치·기상 임계값, 이벤트·피드백 세부 정책, 보존·삭제 정책.
- 미확정 항목 중 구현 기본값으로 해결 가능한 것은 근거를 문서화하고 설정으로 분리한다. 개인정보 수집·비용·권한·외부 변경이 필요한 결정은 임의 확정하지 않는다.
