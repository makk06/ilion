# ILION 개발 계획 및 협업 규칙

> 실시간 혼잡도 기반 장소 추천 앱 **ILION**의 개발 기준서입니다.  
> 모든 팀원은 개발을 시작하기 전에 이 문서를 읽고, 규칙이 바뀌면 이 문서도 함께 수정합니다.

## 1. 프로젝트 개요

- 프로젝트명: ILION
- GitHub 저장소: `https://github.com/makk06/ilion`
- 핵심 기능: 실시간 혼잡도와 사용자 조건을 반영한 장소 추천
- 필수 데이터: 한국관광공사 API
- 프런트엔드: Flutter
- 백엔드: Django 6.0.6 · Django REST Framework · SQLite (Python 3.12+)
- 지원 대상: Android 우선, 필요에 따라 iOS 및 Web 확장

## 2. 저장소 구조

Git은 `2026_tourist_congestion_app` 전체에 한 번만 연결합니다.

```text
2026_tourist_congestion_app/
├─ tourist_congestion_frontend/   # Flutter 앱
│  ├─ lib/
│  ├─ android/
│  ├─ ios/
│  ├─ web/
│  └─ pubspec.yaml
├─ tourist_congestion_backend/    # 백엔드 서버
│  ├─ places/                    # 장소·공급자 관측·전국 혼잡도 추정
│  └─ users/                     # 계정·활동 API
├─ .gitignore
├─ README.md
└─ DEVELOPMENT_GUIDE.md
```

### 폴더 규칙

- 프런트엔드 코드는 `tourist_congestion_frontend` 안에서만 관리합니다.
- 백엔드 코드는 `tourist_congestion_backend` 안에서만 관리합니다.
- 프로젝트 전체에 `.git` 폴더는 하나만 둡니다.
- 프런트엔드나 백엔드 폴더 안에서 다시 `git init`을 실행하지 않습니다.
- 자동 생성 파일과 개인 설정 파일은 Git에 올리지 않습니다.

## 3. Git 브랜치 규칙

### 기본 원칙

- `main`은 항상 실행 가능한 안정 버전으로 유지합니다.
- `main`에서 직접 개발하거나 직접 `push`하지 않습니다.
- 기능 또는 작업 하나당 브랜치 하나를 만듭니다.
- 작업이 끝나면 Pull Request를 통해 `main`에 합칩니다.
- 사람 이름이 아니라 작업 내용을 기준으로 브랜치 이름을 정합니다.

### 브랜치 이름

| 구분 | 형식 | 예시 |
| --- | --- | --- |
| 새 기능 | `feature/기능명` | `feature/home-ui` |
| 오류 수정 | `fix/오류명` | `fix/search-crash` |
| 화면 디자인 수정 | `design/화면명` | `design/map-screen` |
| 문서 수정 | `docs/문서명` | `docs/api-guide` |
| 코드 정리 | `refactor/대상` | `refactor/place-model` |
| 개발 환경 설정 | `chore/작업명` | `chore/flutter-config` |

브랜치 이름에는 영문 소문자와 하이픈을 사용합니다.

## 4. 한 작업의 전체 순서

### 4.1 작업 시작

먼저 `main`을 최신 상태로 받습니다.

```bash
git switch main
git pull origin main
```

그다음 작업 브랜치를 생성합니다.

```bash
git switch -c feature/home-ui
```

### 4.2 개발 중 저장

현재 변경 내용을 확인합니다.

```bash
git status
```

관련된 변경만 모아 커밋합니다.

```bash
git add .
git commit -m "feat: 홈 화면 기본 UI 구현"
```

작업 브랜치를 GitHub에 올립니다.

```bash
git push -u origin feature/home-ui
```

같은 브랜치를 다시 올릴 때는 `git push`만 실행하면 됩니다.

### 4.3 작업 완료

1. GitHub에서 해당 브랜치의 Pull Request를 생성합니다.
2. 합칠 방향이 `main ← 작업 브랜치`인지 확인합니다.
3. 작업 내용과 확인 방법을 작성합니다.
4. 다른 팀원 한 명 이상이 코드를 확인합니다.
5. 문제가 없으면 `main`에 병합합니다.
6. 병합된 브랜치는 GitHub에서 삭제합니다.

### 4.4 병합 후 정리

```bash
git switch main
git pull origin main
git branch -d feature/home-ui
```

다음 작업은 최신 `main`에서 새 브랜치를 만들어 시작합니다.

## 5. 커밋 메시지 규칙

커밋 메시지는 `종류: 변경 내용` 형식으로 작성합니다.

| 종류 | 의미 | 예시 |
| --- | --- | --- |
| `feat` | 새 기능 | `feat: 혼잡도 필터 추가` |
| `fix` | 오류 수정 | `fix: 검색 결과 중복 표시 수정` |
| `design` | UI·스타일 변경 | `design: 홈 카드 간격 조정` |
| `refactor` | 기능 변화 없는 코드 정리 | `refactor: 장소 모델 구조 정리` |
| `docs` | 문서 변경 | `docs: 실행 방법 추가` |
| `test` | 테스트 추가·수정 | `test: 추천 API 테스트 추가` |
| `chore` | 설정·패키지·기타 작업 | `chore: Flutter 패키지 업데이트` |

### 커밋 원칙

- 한 커밋에는 하나의 목적만 담습니다.
- `수정`, `작업함`, `완료`처럼 내용을 알 수 없는 메시지는 사용하지 않습니다.
- 실행되지 않는 중간 코드는 가능한 한 원격 저장소에 올리지 않습니다.
- 다른 사람의 변경을 임의로 되돌리지 않습니다.

## 6. Pull Request 규칙

### 제목

```text
[Frontend] 홈 화면 기본 UI 구현
[Backend] 장소 추천 API 추가
[Fix] 검색 결과 중복 오류 수정
```

### 본문 양식

```markdown
## 작업 내용
- 홈 화면 검색창 구현
- 혼잡도 표시 카드 추가

## 확인 방법
1. 앱을 Chrome에서 실행
2. 홈 탭 진입
3. 검색창과 혼잡도 카드 표시 확인

## 참고 사항
- 현재 데이터는 임시 데이터임

## 스크린샷
- UI 변경이 있다면 첨부
```

### 병합 전 확인

- 앱 또는 서버가 정상적으로 실행되는가?
- 기존 기능이 깨지지 않았는가?
- API 키, 비밀번호, 개인정보가 포함되지 않았는가?
- 불필요한 자동 생성 파일이 포함되지 않았는가?
- UI 변경이라면 스크린샷이 첨부되었는가?
- 리뷰 의견이 반영되었는가?

## 7. 충돌을 줄이는 규칙

- 같은 파일의 같은 부분을 두 명이 동시에 수정하지 않습니다.
- 작업을 시작하기 전에 담당 파일과 기능을 팀 채널에 공유합니다.
- 큰 기능은 작은 작업으로 나누어 자주 Pull Request를 만듭니다.
- 오래 작업한 브랜치는 중간에 최신 `main` 내용을 반영합니다.
- 충돌이 발생하면 코드를 작성한 팀원들과 함께 확인한 후 해결합니다.
- 이해하지 못한 상태에서 충돌 표시를 지우거나 강제로 덮어쓰지 않습니다.

## 8. 프런트엔드 규칙

### 기본 구조

기능이 늘어나면 `lib` 폴더를 다음과 같이 정리합니다.

```text
lib/
├─ main.dart
├─ screens/       # 화면 단위 위젯
├─ widgets/       # 여러 화면에서 재사용하는 위젯
├─ models/        # 데이터 모델
├─ services/      # API 호출 및 외부 서비스 연결
├─ providers/     # 상태 관리
├─ constants/     # 색상, 문자열, API 경로 등 공통 값
└─ utils/         # 공통 보조 함수
```

### 작성 원칙

- 한 파일이 지나치게 커지면 화면, 위젯, 서비스 단위로 분리합니다.
- 같은 UI를 두 번 이상 사용하면 공통 위젯으로 분리합니다.
- 화면 코드에서 API를 직접 호출하지 않고 `services`를 거칩니다.
- 색상과 반복 문자열은 가능한 한 공통 상수로 관리합니다.
- 임시 데이터에는 `TODO: 실제 API 연결 후 제거` 주석을 남깁니다.
- UI 변경 후 Chrome 또는 Android 환경에서 직접 확인합니다.

### 기본 검사 명령

```bash
flutter pub get
flutter analyze
flutter test
flutter run -d chrome
```

## 9. 백엔드 규칙

백엔드 기술이 확정되면 이 항목에 실행 방법과 폴더 구조를 추가합니다.

현재 공통 규칙은 다음과 같습니다.

- 프런트엔드에 한국관광공사 API 키를 직접 넣지 않습니다.
- 외부 API 호출, 데이터 정제, 혼잡도 계산은 백엔드에서 담당합니다.
- API 응답 형식은 프런트엔드 담당자와 합의한 뒤 변경합니다.
- 오류 응답에는 일관된 상태 코드와 메시지 형식을 사용합니다.
- 사용자에게 보여줄 정보와 서버 내부 오류 정보를 구분합니다.
- API가 변경되면 관련 문서와 프런트엔드 담당자에게 알립니다.

권장 응답 형식의 예시는 다음과 같습니다.

```json
{
  "success": true,
  "data": {},
  "message": ""
}
```

## 10. 프런트엔드와 백엔드 연결 규칙

새 API를 만들기 전에 다음 내용을 먼저 정합니다.

| 항목 | 예시 |
| --- | --- |
| 기능 | 장소 검색 |
| 요청 방식 | `GET` |
| 경로 | `/api/places` |
| 요청 값 | `keyword`, `latitude`, `longitude` |
| 성공 응답 | 장소 ID, 이름, 위치, 혼잡도 |
| 오류 응답 | 상태 코드, 오류 메시지 |
| 담당자 | 프런트엔드 1명, 백엔드 1명 |

- 합의된 API 명세는 별도 문서에 기록합니다.
- 실제 서버가 준비되지 않았다면 프런트엔드는 합의된 형식의 임시 데이터를 사용합니다.
- 필드 이름을 변경할 때는 프런트엔드와 백엔드를 함께 수정합니다.
- 날짜와 시간의 형식, 좌표 단위, 혼잡도 단계 기준을 미리 통일합니다.

## 11. 환경 변수와 보안

다음 정보는 절대 GitHub에 올리지 않습니다.

- 한국관광공사 API 키
- 데이터베이스 주소, 계정, 비밀번호
- 로그인 토큰과 인증서
- 개인 전화번호, 이메일 등 실제 사용자 정보
- 배포 서비스의 비밀 키

비밀값은 `.env` 같은 환경 변수 파일에 저장하고 `.gitignore`에 등록합니다. 팀원에게는 실제 값이 없는 예시 파일만 공유합니다.

```text
.env              # Git에 올리지 않음
.env.example      # 변수 이름만 기록하여 Git에 올림
```

API 키가 실수로 GitHub에 올라갔다면 파일만 삭제하지 말고, 즉시 해당 키를 폐기한 뒤 새 키를 발급받습니다.

## 12. 작업 관리 규칙

모든 기능은 개발 전에 다음 정도로 작게 나눕니다.

```text
기능명: 홈 화면 혼잡도 지도
담당자: 홍길동
브랜치: feature/congestion-map
완료 조건:
- 지도 영역이 표시됨
- 위치 권한 거부 시 안내가 표시됨
- 혼잡도 단계별 색상이 구분됨
관련 작업: 백엔드 혼잡도 API
```

### 상태 구분

- `할 일`: 아직 시작하지 않은 작업
- `진행 중`: 담당자가 브랜치를 만들어 작업 중
- `검토 중`: Pull Request가 열린 상태
- `완료`: `main`에 병합되고 확인이 끝난 상태

한 사람은 동시에 너무 많은 작업을 맡지 않고, 진행 중인 작업을 먼저 끝낸 뒤 다음 작업을 시작합니다.

## 13. 회의 및 의사결정 규칙

- 회의 전에 논의할 주제를 공유합니다.
- 기능, 일정, API 형식처럼 개발에 영향을 주는 결정은 글로 남깁니다.
- 결정에는 날짜, 결정 내용, 이유, 관련 담당자를 기록합니다.
- 이미 확정된 내용을 바꾸려면 영향받는 담당자와 먼저 상의합니다.
- 회의에서 정하지 못한 내용은 담당자와 결정 기한을 지정합니다.

간단한 기록 양식은 다음과 같습니다.

```text
날짜: 2026-08-06
결정: 혼잡도를 여유/보통/혼잡 3단계로 표시
이유: 사용자에게 빠르게 상태를 전달하기 위해
영향 범위: 프런트엔드 지도, 추천 API, 데이터베이스
담당자: OOO, OOO
```

## 14. 기능 완료 기준

다음 조건을 모두 만족해야 해당 작업을 완료한 것으로 봅니다.

- 요구한 기능이 동작합니다.
- 실행 중 치명적인 오류가 없습니다.
- 기본적인 예외 상황을 처리했습니다.
- 코드 검사와 가능한 테스트를 통과했습니다.
- 비밀 정보와 불필요한 파일이 포함되지 않았습니다.
- 필요한 문서가 갱신되었습니다.
- Pull Request 리뷰를 받고 `main`에 병합했습니다.

## 15. 초기 개발 순서

1. GitHub 저장소와 팀원 권한 설정
2. Flutter 기본 화면 및 화면 이동 완성
3. 백엔드 기술과 데이터베이스 확정
4. 한국관광공사 API 호출 테스트
5. 장소 데이터의 공통 형식 확정
6. 프런트엔드용 임시 데이터 구조 통일
7. 장소 검색 API 연결
8. 혼잡도 수집·계산 방식 구현
9. 추천 기준과 추천 API 구현
10. 저장, 로그인, 마이페이지 기능 연결
11. 오류 처리, 테스트, 디자인 보완
12. Android 앱 배포 준비

## 16. 아직 정해야 할 사항

- 백엔드 언어와 프레임워크
- 데이터베이스 종류
- 사용자 로그인 방식
- 실시간 혼잡도 데이터의 출처와 계산 기준
- 추천 알고리즘의 입력값과 우선순위
- 지도 서비스와 위치 API
- 배포 서버 및 운영 방식
- 팀원별 첫 담당 기능과 일정

결정된 내용은 회의 기록에만 두지 말고 이 문서의 해당 항목에도 반영합니다.

## 17. 가장 중요한 다섯 가지

1. `main`에서 직접 작업하지 않습니다.
2. 작업 전 `main`을 최신 상태로 받고 새 브랜치를 만듭니다.
3. 기능 하나가 끝나면 Pull Request로 검토 후 병합합니다.
4. API 키와 비밀번호는 절대 GitHub에 올리지 않습니다.
5. 팀이 합의한 변경 사항은 반드시 문서에 남깁니다.


## 전국 혼잡도 개발 기준

[혼잡도 운영 안내](docs/crowd-estimation.md)를 따른다. 공급자 관측과 자체 추정을 구분하며 외부 호출은 수집 명령에서만 수행한다. Django와 Flutter 테스트를 함께 실행한다. 기존 사용자 변경을 보존하는 additive migration을 사용한다.
