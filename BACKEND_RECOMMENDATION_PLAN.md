# 추천 알고리즘 및 백엔드 개발 계획

> 2026-09-12 갱신 안내: 최신 제품 범위·수집 계약은 [전국 추천 MVP 최종 계획](./BACKEND_MVP_PLAN.md)을 따른다.
> 아래는 이전 초안이다. 특히 최대 3개 → 기본 최대 10개, 날씨 후속 → MVP 필수,
> 전국 기본 추천, 요청 시 공통 수집 서비스를 통한 보충 수집으로 변경되었다.
> 로컬은 SQLite이며 운영 DB는 미정이다. 충돌 시 최신 계획을 우선한다.

## 1. 담당 범위

이 문서는 외부 API 수집 담당자가 정규화하여 저장한 장소·혼잡도 데이터를 바탕으로 추천 알고리즘과 추천 API를 구현하기 위한 작업 계획이다.

추천 백엔드의 책임은 다음과 같다.

- DB에 저장된 장소, 혼잡도, 사용자 선호 데이터 조회
- 추천 후보 필터링 및 점수 계산
- 추천 결과와 추천 이유를 프론트엔드에 제공
- 특정 장소를 기준으로 한 대체 장소 추천
- 추천 노출·클릭 등 행동 데이터 기록 기반 마련

외부 관광·인구·혼잡도 API 호출, 원본 응답 저장, 데이터 정규화와 주기적 수집은 데이터 수집 담당 범위로 둔다. 사용자 요청을 처리하는 추천 API 안에서 외부 API를 실시간 호출하지 않는다.

## 2. 브랜치와 개발 순서

기능을 너무 잘게 나누지 않고, 실행 가능한 단위별로 순차 개발한다.

```text
main
 └─ feature/be-recommendation-core
      ↓ main 병합
 └─ feature/be-place-alternatives
      ↓ main 병합
 └─ feature/be-recommendation-events
```

현재 브랜치인 `feature/be-recommendation-core`에서는 핵심 추천 API와 테스트까지만 완성한다. 이 브랜치가 `main`에 병합된 뒤 최신 `main`에서 다음 브랜치를 만든다.

## 3. 1차 구현: 핵심 추천 API

### API 초안

```http
POST /api/recommendations
```

요청 예시:

```json
{
  "latitude": 37.5665,
  "longitude": 126.978,
  "radius_km": 5,
  "categories": ["자연", "문화"],
  "desired_crowd_levels": ["relaxed", "normal"],
  "indoor_outdoor": "outdoor",
  "stay_minutes": 120,
  "limit": 3
}
```

응답 예시:

```json
{
  "success": true,
  "data": {
    "recommendation_request_id": "uuid",
    "items": [
      {
        "rank": 1,
        "place": {
          "id": 15,
          "name": "서울숲",
          "category": "자연"
        },
        "distance_km": 1.4,
        "score": 87.3,
        "score_confidence": 0.75,
        "score_breakdown": {
          "crowd": 92,
          "preference": 100,
          "distance": 68,
          "context": null,
          "discovery": null
        },
        "reasons": [
          "현재 비교적 한적합니다",
          "선호한 자연 카테고리에 해당합니다",
          "현재 위치에서 가깝습니다"
        ]
      }
    ]
  },
  "message": ""
}
```

### 후보 장소 선정

- 위도·경도와 검색 반경을 검증한다.
- 기존 `/api/places/nearby`의 bounding box 및 Haversine 거리 계산을 공통 서비스로 추출하여 재사용한다.
- `PlaceSource`가 존재하는 장소는 `matched` 상태의 소스가 있는 경우만 노출한다.
- 카테고리, 실내·실외 조건을 적용한다.
- 요청 반경 밖의 장소를 제외한다.
- 영업 상태가 `CLOSED`인 장소를 어떻게 다룰지는 프론트엔드 계약 확정 후 필터 또는 감점으로 적용한다.
- 후보가 없을 때는 서버 오류가 아니라 빈 `items`를 반환한다.

### 사용자 선호 적용

- 요청의 `categories`가 있으면 요청값을 우선한다.
- 요청값이 없고 로그인 사용자라면 `User.preferred_categories`를 사용한다.
- 비로그인 사용자와 선호 정보가 없는 사용자도 추천을 받을 수 있어야 한다.
- 클라이언트에서 전달한 선호 카테고리 값과 DB의 `Place.category` 값이 동일한 분류 체계를 사용하도록 사용자 기능 담당자와 계약한다.

## 4. 추천 점수 초안

회의에서 논의한 시작 비중은 다음과 같다.

| 항목 | 비중 | 1차 구현 여부 |
|---|---:|---|
| 혼잡도 적합도 | 35% | 구현 |
| 사용자 선호도 | 20% | 구현 |
| 거리 | 20% | 구현 |
| 영업시간·날씨 등 상황 | 15% | 데이터 계약 후 구현 |
| 새로운 장소 발견 요소 | 10% | 행동 데이터 축적 후 구현 |

모든 세부 점수는 `0~100` 범위로 통일한다. 데이터가 없는 항목은 0점으로 간주하지 않고 계산에서 제외한다.

```text
최종 점수 = Σ(사용 가능한 세부 점수 × 비중) / 사용 가능한 비중의 합
점수 신뢰도 = 사용 가능한 비중의 합
```

예를 들어 혼잡도·선호도·거리만 계산했다면 사용 가능한 비중은 `0.75`이고, 응답의 `score_confidence`도 `0.75`가 된다.

### 세부 규칙

- 혼잡도 `unknown`은 한적함으로 취급하지 않고 혼잡도 점수를 계산하지 않는다.
- 사용자가 원하는 혼잡도 단계와 가까울수록 높은 점수를 준다.
- 요청 카테고리 또는 저장된 선호 카테고리와 일치하면 선호 점수를 준다.
- 거리 점수는 검색 반경을 기준으로 가까울수록 높아지는 단순 감쇠식으로 시작한다.
- 동일한 입력은 동일한 결과 순서를 반환해야 한다.
- 동점은 최신 혼잡도 데이터, 짧은 거리, 작은 `Place.id` 순으로 정렬한다.
- 세부 점수와 추천 이유를 함께 반환해 결과를 설명할 수 있게 한다.

점수표와 거리 감쇠식의 정확한 값은 상수 또는 설정으로 분리하여 회의 후 쉽게 수정할 수 있게 한다.

## 5. 권장 코드 구조

추천 기능은 향후 행동 기록과 대체 장소 추천까지 확장되므로 별도 Django 앱으로 분리한다.

```text
tourist_congestion_backend/recommendations/
├── apps.py
├── serializers.py
├── urls.py
├── views.py
├── services/
│   ├── candidate_selector.py
│   ├── scorers.py
│   └── recommendation_service.py
└── tests/
```

핵심 서비스 진입점 예시:

```python
RecommendationService.recommend(
    query,
    user=request.user,
    reference_place=None,
)
```

HTTP 요청 검증과 응답 조립은 serializer/view가 담당하고, 후보 선정과 점수 계산은 서비스 계층에서 담당한다. 점수 계산기는 DB나 HTTP 요청 객체에 의존하지 않는 순수 함수에 가깝게 만들어 단위 테스트하기 쉽게 한다.

## 6. 데이터 수집 담당자와 맞출 DB 계약

추천 기능에서는 외부 제공처마다 다른 원본 필드 대신 아래 정규화된 필드를 사용한다.

- 장소 ID, 이름, 카테고리, 하위 카테고리
- 주소, 위도, 경도, 지역 코드
- 실내·실외 구분
- 영업 상태 또는 영업시간
- 정규화된 혼잡도 단계: `relaxed`, `normal`, `busy`, `crowded`, `unknown`
- 가능하면 0~100 범위의 혼잡도 점수
- 혼잡도 관측 시각
- 데이터 제공처
- 대체 데이터 여부와 최신성 판단에 필요한 정보

현재 모델에서 우선 사용할 값은 `Place`, `PlaceInfo`, `PlaceSource`, `PlaceCrowdArea`, `CrowdData`이다. 예보 데이터나 전국 단위 혼잡도 데이터에 새 필드가 필요하면 추천 로직보다 먼저 수집 담당자와 모델 계약을 변경한다.

## 7. 테스트 및 완료 기준

`feature/be-recommendation-core`의 완료 기준은 다음과 같다.

- 정상 요청에서 추천 결과를 최대 3개 반환한다.
- 위도·경도·반경·limit·필터값의 오류를 검증한다.
- 반경 밖 장소와 비공개 상태 장소를 제외한다.
- 카테고리와 실내·실외 필터가 동작한다.
- 익명 사용자의 요청이 동작한다.
- 요청 카테고리가 없으면 로그인 사용자의 선호 카테고리를 적용한다.
- `unknown` 혼잡도를 한적함으로 취급하지 않는다.
- 누락 데이터의 비중을 재정규화하고 신뢰도를 반환한다.
- 동점 정렬이 항상 동일하다.
- 후보가 없으면 성공 응답과 빈 목록을 반환한다.
- 추천 이유와 점수 상세가 실제 계산 결과와 일치한다.
- 기존 장소·사용자 API 테스트가 함께 통과한다.

## 8. 후속 브랜치

### `feature/be-place-alternatives`

```http
GET /api/places/{place_id}/alternatives
```

- 기준 장소 자체는 제외한다.
- 동일하거나 유사한 카테고리를 우선한다.
- 가까운 장소를 우선한다.
- 데이터가 있으면 더 낮은 혼잡도를 우선한다.
- 핵심 `RecommendationService`를 재사용한다.

### `feature/be-recommendation-events`

- 추천 목록 노출
- 장소 상세 클릭
- 즐겨찾기
- 길찾기
- 추천 결과 선택
- 이후 피드백 연결

행동 데이터는 우선 기록만 하고 1차 추천 점수에는 반영하지 않는다. 저장할 위치 정보의 정밀도, 보존 기간, 익명 사용자 식별 방식은 구현 전에 팀 계약을 확정한다.

## 9. 1차 구현에서 제외하는 항목

- 외부 API 직접 수집 및 배치 작업
- 날씨 기반 점수
- 예측 혼잡도 기반 시간대 추천
- 사용자 피드백을 학습한 개인화 가중치
- 보상 시스템
- 운영 대시보드

위 항목은 핵심 추천 API가 안정적으로 동작하고 필요한 데이터가 확보된 뒤 별도 브랜치에서 진행한다.
